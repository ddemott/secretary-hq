# SecretaryHQ — Multi-Tenant Voice AI Reception SaaS

## Project Overview

Multi-tenant AI receptionist for service businesses (tire shops, salons, auto shops, trades, fitness, food & beverage).

**HIPAA verticals are permanently excluded** (medical, dental, chiropractic, optometry, veterinary).

Completed phases live in `docs/planning/RESOLVED.md`. Current tasks in `docs/planning/TODO.md`. Vertical-preset execution order in `docs/planning/ROADMAP.md` (steps 1–10 closed in CI). The former cross-angle gap inventory `docs/GAPS.md` was folded into `docs/planning/TODO.md` (2026-07-05; archived verbatim in `docs/planning/RESOLVED.md`). Verification blind spots — the "green CI but broken in prod" seams (missing integration tests, tool-selection evals, observability) — are a **section of `docs/planning/TODO.md`** ("Verification blind spots", folded 2026-07-02 from the former `docs/TODO_BLINDSPOT.md`). Framework-migration history (Vapi → LiveKit, Edge Functions → Fastify, OpenAI TTS → xAI Grok (2026-05) then full reversal to OpenAI TTS 2026-06-25) in `docs/voice/FRAMEWORK_MIGRATIONS.md`. Hard-won debugging lessons (incl. the voice-agent go-live) in `docs/workflow/LESSONS_LEARNED.md`. (Reference docs live under `docs/`. The root is NOT clean and this doc claimed it was until 2026-08-14 — tracked root markdown is `CLAUDE.md`, `README.md`, `AGENTS.md`, `DEMO_SECTION.md`, `DEVELOPMENT_WORKFLOW.md`, `HANDOFF.md`, plus the raw live-call evidence files `CALL_IMPROVEMENTS.md` / `CALL1.md` / `CALL2.md`. The call files are deliberately at root and deliberately raw: they are the transcripts + persisted tool traces that the fix batches in `docs/planning/TODO.md` cite as evidence, and they stay put until their batches close. `AGENTS.md` is NOT a workflow doc — it is the response-style rule other agent CLIs pick up (mirrored in `.clinerules/`, `.windsurf/rules/`, `.opencode/`). Root `DEVELOPMENT_WORKFLOW.md` is NOT a copy of `docs/workflow/DEVELOPMENT_WORKFLOW.md` either — it is a much shorter local-setup + `test_db` bootstrap note (`scripts/setup-test-db.ts`, `app_user` with `NOBYPASSRLS`); the long process doc is the one under `docs/`. Two files, one name, different content: read the path, not the name. `BRANCH_CHECKLIST.md` at root is UNTRACKED scratch — the tracked one is `docs/workflow/BRANCH_CHECKLIST.md`. `portable-workflow-kit/` is the extractable copy of the workflow, paired with `workflow.config.json`.)

## Architecture

- **SMS is OFF (`ENABLE_SMS`, default false)** until 10DLC registration lands. Not one text this product has sent has ever reached a handset — the number is not registered, the carriers drop everything, and Telnyx reports success anyway (error 40010). Meanwhile the agent was PROMISING texts on real calls ("you'll receive a confirmation shortly"). With SMS off it has no `record_sms_consent` or `send_self_service_link` tools (the latter refiled from `scheduling` to `sms` 2026-07-17 — mis-filed, it escaped the gate and could truthfully relay Telnyx's false "sent" for a link text that died at the carrier) and the prompt tells it plainly that it cannot text: **it cannot promise what it has no means to do.** (Known residual of the same class: `page_owner_via_sms`'s urgent-text promise — the message row persists but no text arrives until 10DLC.) Each tenant needs their OWN 10DLC brand + campaign + number (the perceived SENDER must be the registered brand — CTIA; a shared platform brand gets the traffic blocked), so onboarding must collect legal name/EIN/address/website and provision per-tenant. ~$12-13/tenant/month, 1-3 business days.
- **Voice**: Telnyx → LiveKit Cloud → LiveKit Agent (Node) → Deepgram (STT) + OpenAI (LLM) + **Deepgram Aura (TTS, native WebSocket streaming — switched from OpenAI TTS 2026-07-14 because the OpenAI plugin is NON-STREAMING: it buffers the whole reply before emitting any audio, so every turn was silence-then-a-burst. Chopping the input into sentences with StreamAdapter only traded one gap for a gap between every sentence. You cannot make a non-streaming engine stream by chopping its input finer)** → Fastify `/agent-tools/*`
- **Backend**: Fastify (29 top-level route modules under `src/routes/` + the `agentTools/` module dir, registered from `src/index.ts`) → Postgres (Railway)
- **Agent worker**: `agent/` package on Railway as `secretary-hq-agent`. Single worker per tenant; tenant_id flows in via SIP dispatch metadata.
- **Dashboard**: Next.js 16 (App Router) + Tailwind + TS
- **Database**: Postgres + pgvector, RLS multi-tenancy, atomic booking RPCs
- **Async**: Mostly inline in routes (post-call summaries, calendar sync, SMS triggers). `src/workers/reminderScheduler.ts` polls `reminder_schedules` every 60s.
- **Auth**: JWT (8h, auto-logout on 401), bcrypt

## Tech Stack

- **Backend**: Fastify 5, bcrypt, zod, pg
- **Frontend**: Next.js 16, React 19, Tailwind 3.4, Lucide, react-big-calendar
- **Voice agent**: LiveKit Agents (Node), `@livekit/agents-plugin-{deepgram,openai}`, `livekit-server-sdk`
- **DB**: PostgreSQL + pgvector (ankane/pgvector Docker)
- **Voice stack**: Telnyx + LiveKit Cloud + Deepgram Nova-3 (STT) + OpenAI GPT-4.1-mini (LLM; moved up from GPT-4o-mini 2026-07-20 — 4o-mini reliably failed the third-person one-breath-message eval case and never scored a perfect suite; 4.1-mini passed it every run at the same mini-class latency/cost tier. Auxiliary summary/classify/fallback calls stay on 4o-mini) + **Deepgram Aura (TTS, default `aura-asteria-en`)**. TTS moved OpenAI → Deepgram Aura on **2026-07-14**: the OpenAI TTS plugin is NON-STREAMING (buffers the entire reply before emitting any audio), so every turn was silence-then-a-burst. Aura streams over a WebSocket as the words are produced. (Grok/xAI TTS was removed 2026-06-25; no `XAI_API_KEY` needed. The LLM is still OpenAI.) **Open, unresolved (2026-08-14, working tree): Aura's WebSocket `speak` path returned ZERO audio bytes from the local dev host while the HTTP `collect` path returned audio on the same key and voice.** So the new `agent/src/greetingPickup.ts` (a) makes the greeting play a COLLECTED frame rather than a live stream and (b) adds `AURA_TTS_STREAMING=false` as an escape hatch that puts session replies on the collect path too. **Prod keeps the streaming default and has NOT been shown to have this fault** — the measurement is one host, and treating it as a platform-wide Aura outage would be over-claiming. What is not in doubt: a silent line is indistinguishable from a dead line to the caller, so `verify:tts` remains mandatory before any TTS change ships. Voice + speed are **per-tenant** via `tenants.tts_voice` (an OpenAI voice id: shimmer/nova/alloy/echo/onyx/fable) `/tts_speed`, set on the dashboard Phone Assistant → AI Persona page. The five style flags (`tts_soft`, `tts_cheerful`, `tts_formal`, `tts_warm`, `tts_concise`) are **LIVE, not inert** — this doc claimed they were dead Grok prosody columns until 2026-07-13, which was false and dangerous next to the "delete on sight" principle. They began as Grok prosody flags and were repurposed as LLM **prompt-style** flags: live dashboard toggles (`VoiceIdentitySection.tsx`) → `tenants` UPDATE → `/agent-tools/tenant-config` → `agent/src/prompt.ts` `# Voice style`. Deleting them removes two working toggles.
- **Testing**: Vitest (backend + dashboard), Playwright (e2e), `scripts/simulate.sh` (on-demand system harness: `status` health board, `tools` agent-tools journey, `toolselect` LLM tool-selection eval, `call` browser voice test — see Development)

## Key Directories

Items below capture hidden context — things you can't grep for. Everything else (flat service files, type definitions, doc tree) is derivable from the filesystem.

- `/src` — Fastify backend (slim `index.ts` + 29 top-level route modules + `routes/agentTools/`)
- `/src/routes/routeHelpers.ts` — `sendValidationError`, `sendNotFound`, `sendSuccess`, `sendConflict`, `assertRowAffected`, `requireValidUUID`, `parseDateRange`, `parsePagination`
- `/src/routes/agentTools/` — the 29 `/agent-tools/*` routes the voice agent calls (plus `_test/sync-events`, which the agent never calls), split by concern 2026-07-11 (was a single 2,517-line `agentTools.ts`). `index.ts` owns ONLY the shared `x-agent-secret` auth hook + registration order; `schemas.ts` (Zod) and `helpers.ts` (`ok`/`fail`/`toolRoute`/`pgErrorFields`/interval math + the `AgentToolDeps` plumbing interface every module destructures) are shared. Route modules: `session` (tenant-config + voice-session lifecycle), `identity` (identify/lookup/history, preferences, consent, OTP verification), `scheduling` (service-catalog, availability, both booking RPCs, cancel/reschedule), `knowledge` (RAG policy-answer — the only module touching embeddings), `messaging` (take-message, page-owner, job inquiry, self-service link — the only module touching ConsentService/SMSService), `aiCost`, `_testRoutes` (SYNC_TEST_RECORDER readout). Grouped by what a route is ABOUT, not the mechanism it uses — so the OTP pair lives in `identity` (it establishes who the caller is) and `send-self-service-link` in `messaging` (an outbound contact), not with their transport. Importers use the directory path (`./routes/agentTools`), which resolves to `index.ts`. Note `tests/routes/available-slots.test.ts` reads the route file OFF DISK to assert the route exists — it points at `scheduling.ts`; a future split must update it.
- `/src/services/communications/` — CommunicationService + email/sms/appointment services + Handlebars templates + ProviderRegistry (Telnyx + Mock). Consent-gated.
- `/src/services/reminders/` — `index.ts` (`ReminderService`) is the WHOLE live implementation: it schedules, checks consent, delivers via CommunicationService, and emits `reminders_sent_total` / `reminders_skipped_total`. The worker that drives it is `/src/workers/reminderScheduler.ts`. A second, parallel implementation (`reminderProcessor.ts` + `reminderScheduler.ts` + `reminderRepository.ts`, 391 lines, zero production callers) was **deleted 2026-08-20** — the name collision with the live worker is what hid it for months, and its own green metrics test was asserting a `channel` label production has never emitted.
- `/src/database/index.ts` — Canonical pool (lazy singleton w/ deadlock-prevention timeouts: `statement_timeout=30000`, `lock_timeout=10000`, `idle_in_transaction_session_timeout=60000`, `max=10`, `connectionTimeoutMillis=5000`). The first three are Postgres server-side GUCs (`options` string); `connectionTimeoutMillis` is the client-side checkout cap — added 2026-05-21 so a request that can't get a pool slot under load fails fast (→ error → `errors_total`) instead of hanging forever. `createWithTenantClient(pool)` returns the per-request RLS-scoped helper injected into routes.
- `/src/workers/reminderScheduler.ts` — 60s tick, batches up to 100. Runs in prod or when `ENABLE_REMINDER_SCHEDULER=true`.
- `/src/workers/voiceSessionReaper.ts` — 60s tick; calls the `reap_stale_voice_sessions()` SECURITY DEFINER RPC to force-finalize any `voice_sessions` left `status='active'` past `VOICE_SESSION_REAP_MINUTES` (default 15), computing duration from `started_at`. Backstop so every call has a completed record even if the agent never sends voice-session-end (the agent finalizes on the session `Close` event; this catches what slips through). Runs in prod or when `ENABLE_VOICE_SESSION_REAPER=true`. Nonzero reaps bump `errors_total{event="voice_session_reaped"}`.
- `/src/templates/` — 5 industry YAML bundles (automotive_v1, salon_v1, mobile_tire_v1, auto_bays_v1, ai_platform_v1). No HIPAA verticals.
- `/shared` — Pure cross-runtime TypeScript modules (no Node/Next/framework deps) intended to be consumed from both the Fastify backend (`../shared/...` from `src/`) and the Next.js dashboard (`../../shared/...` from `dashboard/lib/`). Residents: `getEmbedding.ts`, `normalizeForEmbedding.ts`, `scheduling.ts`, `voiceCrm.ts` (2026-05), `appointmentValidation.ts` (2026-05, 15-min increment + duration rules), `phone.ts` (2026-05-27: normalizePhone + formatPhone + isValidPhone), `name.ts` (2026-05-27: splitName, joinName, buildDisplayName, slugify), and `questionBank.ts` (2026-06-19: POLICY_QUESTIONS/POLICY_CATEGORIES + `resolveQuestions({customs})` for onboarding website-import; moved out of `dashboard/lib/policyQuestions.ts` — now a thin re-export — to kill a brittle backend→dashboard runtime import). These were extracted to kill duplication between backend CRM sync / agent tools and dashboard forms/comboboxes. Later residents, same rule: `callContext.ts`, `expandQueryForEmbedding.ts`, `markerQuestions.ts`, `versionHistoryFields.ts`, and the **checklist config trio the backend and dashboard must agree on byte-for-byte** — `checklistPresetDerivation.ts` (preset id + `business_type` → runtime config), `checklistOverrides.ts` (the SUBTRACT-only validator), `checklistPreview.ts` (the Business Settings next-call dry-run). Those three live here precisely because the dashboard preview and the agent's actual runtime must never disagree about what the next call will ask.
- `/src/middleware/fastify-middleware.ts` — `withHandler`, `tenantMiddleware`, `registerJwtAuthHook`, `generateToken`, `AppError`, `requireTenantId`, `requireAuth`, `requireSuperAdmin`, `logEvent/Warning/Error`. JWT preHandler (PUBLIC_ROUTES bypass + password-rotation check) lives here. `tenantMiddleware` enforces tenant isolation in two layers: (1) any non-public, non-tenant-exempt request with no authenticated session (`req.auth`) is rejected 401 before any tenant resolution — a user-supplied `tenant_id` is a selector within the JWT's permitted tenants, never a substitute for auth (added 2026-05-21 after an anonymous `?tenant_id=<uuid>` was found to return that tenant's data read+write+delete with zero auth; the 2026-05-06 guard only fired when a jwtTenant already existed); (2) for authenticated callers, any user-supplied `tenant_id` (query or body) not matching the JWT's is rejected 403 unless super-admin (added 2026-05-06). `requireTenantId` trusts only the middleware-validated `req.tenantId` (no body fallback). Use `requireSuperAdmin` (not `requireAuth`) on `/tenants/*` and other cross-tenant admin operations.
- `/agent` — LiveKit Agents worker (Node). **`index.ts` picks ONE of three call architectures at session start** (`index.ts` ~L1294) and the choice decides which of the modules below are live code and which are dead weight:
  1. **`ChecklistAgent` (question trees) — THIS IS WHAT PRODUCTION RUNS.** `ENABLE_QUESTION_TREE` is `(v) => v !== 'false'`, i.e. **ON unless explicitly disabled**. See `/agent/src/checklist/` below.
  2. `CallRootAgent` (TaskGroup "rungs", `agent/src/tasks/`) — only when `ENABLE_QUESTION_TREE=false` AND `ENABLE_TASK_GROUP=true`. Superseded 2026-07-21.
  3. `SpeakingAgent` (the prompt LADDER, `prompt.ts` + `tenants.system_prompt`) — only when both flags are off. The original architecture.
     **THE TOOLSET THE MODEL ACTUALLY SEES IS SMALLER THAN `tools.ts`, AND VARIES BY TREE SELECTION.** `tools.ts` defines **26** real tools; under question trees `selectedTools()` presents **12 base** (4 base tools + 5 action tools drawn from the selected trees + 3 passthrough — `set_purpose`, `record_answer`, `finish_call`, `answer_question` which WRAPS `get_company_policy_answer` so RAG is reachable under a different name; `book_with_scheduling`, `take_message`, `capture_job_inquiry`, `cancel_appointment`, `reschedule_appointment`; `get_available_slots`, `get_service_catalog`, `get_my_appointments`) **plus 3 more whenever the `identity` tree is selected** (`get_customer_context`, `send_verification_code`, `verify_phone_code` — wired 2026-08-03; `identity` is selected on every goal-bearing call, so in practice most calls see 15). The rest are **never presented to the model on a live call**. Some are dead by design (`start_booking` / `manage_appointment` were LADDER routers; `book_appointment`, `check_availability`, `get_scheduling_options` are superseded; `record_sms_consent` / `send_self_service_link` are gated off with SMS anyway; `attach_meeting_notes` rides with `buy_service`; `identify_caller` is intentionally host-code-only, invoked directly by `checklistTools.ts`'s `maybeIdentify()`, never model-facing — neither of those two was ever actually a gap despite an earlier version of this doc listing them as one). **Still capability the product believes it has and currently does not** — re-verified 2026-08-03: `transfer_call` (no human handoff exists on a live call), `page_owner_via_sms`, `save_customer_preference`, `get_detailed_customer_history`, `find_caller_by_name` (this one deliberately excluded, not merely unwired — its backend route still has a real enumeration bug, see `docs/planning/TODO.md`). Adding a tool to `tools.ts` does NOT put it in front of the model; it must be an action node in a tree, a `TREE_PASSTHROUGH_TOOLS` entry, or a base tool.
     Shared by all three: `toolsClient`, `sessionContext`, `tools` (the real `/agent-tools/*` bindings), `greeting` (spoken by `index.ts` via `session.say()` BEFORE any agent logic — so greeting changes take effect on every path; **as of 2026-08-14, working tree, the tenant-config fetch AND the greeting TTS warm run BEFORE `waitForParticipant()`, i.e. while the phone is still ringing, because `waitForParticipant` IS pickup and a receptionist does not answer and then go quiet for seconds. `GREETING_POST_PICKUP_WAIT_MS = 0` — the previous cap had been raised to 12s to let a slow local warm finish, and the caller sat in dead air after join and hung up: waiting after pickup is itself the defect**), `transferClient` (live SIP cold-transfer via REFER), `transcript` (TranscriptRecorder → call transcript), `callOutcome` (CallOutcomeTracker → booked/transferred + appointment_id for the call→appointment link), `callSummary` (bounded/failsafe post-call OpenAI summary), `session/*` (watchdog, hold lines, turn detector). `agent/scripts/` holds the simulation helpers — `sim-call.mjs` (LiveKit dispatch + browser join), `sim-call-format.mjs`, `sim-agent-liveness.mjs`, `sim-begincall.ts`, `sim-offscript.ts`, `sim-questiontree.ts`, `sim-taskgroup.ts`, `sim-toolselect.ts` — plus `verify-tts.mjs`, which is the only one that proves the line makes NOISE.
     **Before changing ANY call behaviour, confirm which of the three you are editing.** A prompt sitting in `tenants.system_prompt` is not evidence that a call reads it — under question trees the ladder is never passed to the model. Grep who consumes the thing you are about to edit; that check takes seconds and is the difference between shipping a fix and shipping nothing (2026-07-27: a full block + tests + prod install landed on the ladder while prod ran trees).
- `/dashboard` — Next.js (components/, lib/, app/). Landing at `/`, dashboard at `/dashboard`. Public legal pages at `/privacy`, `/terms`, `/dpa` (shipped 2026-08-14, **working tree — not yet committed**): `components/legal/LegalDocLayout.tsx` owns the shared chrome AND the single source of the legal constants (`LEGAL_EFFECTIVE`, provider = Thinking Hammer LLC dba Secretary HQ, address, `legal@`/`privacy@` contacts). Terms adopt **Bonterms Standard Online Cloud Terms v1.0 by reference** + a Provider-Specific Terms block; the DPA is the **Bonterms DPA v2.0** cover page + an enumerated subprocessor list (Telnyx / LiveKit / Deepgram / OpenAI / Railway / Supabase); Privacy is an ICO-style notice extended with the product's actual call-handling language (calls are answered by an AI, transcribed, and summarized). Linked from the landing footer AND from a **required** consent checkbox on `/register` in which the signer attests they are authorized for the business and that informing callers is THEIR legal duty. **Not lawyer-reviewed** — the Bonterms base is lawyer-drafted, the Provider-Specific Terms are not.
- `/public` — static assets served by the Fastify backend, currently just `caller-simulator.html` (the browser voice-call harness behind `src/routes/callerSimulator.ts`, added 2026-08-10 in #326). The route re-reads the HTML **on every request on purpose** — a cached copy hid an edited page from Playwright while the backend process stayed up, which reads exactly like a broken test.
- `/agent/src/checklist/` — **THE LIVE CALL ARCHITECTURE (question trees).** Three layers, deliberately split:
  - **`trees.ts` + `types.ts` — the questions are DATA.** `PLATFORM_TREE_LIBRARY` holds 10 trees (`identity`, `booking`, `message`, `generic_subject`, `qa`, `job`, `buy_service`, `case_intake`, `schedule_change`, `fix_computer`). **Since 2026-08-14 this library is the TEMPLATE CONTENT + the runtime FALLBACK, not the only source** — a provisioned tenant runs its OWN copy of these trees out of `tenant_question_trees` / `tenant_question_nodes` (migration 20260814130000), delivered as `question_trees` on `/agent-tools/tenant-config` and passed to `ChecklistAgent({ library })`. A tenant with no rows still runs this file. `tests/questionTreeRoundTrip.test.ts` asserts the DB copy is DEEP-EQUAL to the TS tree, which is what makes the ~300 checklist tests that exercise this library valid for the database path too — that equality is the conversion touchstone before flipping any real business over. Node types: `text` (free answer), `choice` (if-branch — answering one option activates its children and rules the siblings `not_applicable`), and `action` (completed ONLY by a real tool's success id, with `requires` as a soft ordering gate and `await_tree` for "finish the intake, THEN write"). Trees never embed each other — a repair call selects `identity + fix_computer + booking` and shared node ids (`caller_name`, `caller_phone`) merge to ONE node. A tree's `description` is written FOR THE PURPOSE SELECTOR and carries the intent boundaries ("can someone fix my computer" is a SERVICE REQUEST, not a job inquiry).
  - **`tracker.ts` — host code owns ALL state.** 10 statuses (`open`/`answered`/`declined`/`not_applicable`/`latent`/`pending`/`blocked`/`ready`/`done`/`unselected`). It computes liveness through choice branches, DISCARDS answers stranded on a dead branch when the caller changes their mind, and renders the checklist injected into the model's context each turn (`[ASK]` = may be asked, `[listen]` = latent, record only if volunteered, `[ACTION NOW]` = call that tool, `[✓]`). **`isResolved()` is THE GOODBYE GATE** — `finish_call` refuses to close while any selected node is unresolved. That gate is the structural guarantee that replaced book-first: sequencing is gone, and a stated goal cannot be forgotten because the call cannot END on it.
  - **`checklistAgent.ts` + `checklistTools.ts` — ONE agent for the whole call, no rungs, no phase swaps.** The model has exactly three jobs: `set_purpose` (pick trees off the menu), `record_answer` (fill anything it hears, in any order), and call the action tool when the checklist says so — plus `answer_question` (RAG) at any moment. The toolset is REBUILT from the current selection (`selectedTools()`): 3 base tools + one wrapped action tool per selected tree + a small passthrough allowlist (`booking` → `get_available_slots`, `get_service_catalog`). `wrapAction` is where the guarantees live: a `done` action REFUSES to repeat (anti-double-book), a `blocked` one names its unmet nodes, omitted args are BACKFILLED from the tracker's recorded answers ("host-owned state that the write ignores is state theater" — a live call dropped `location_type` the checklist showed ✓), completion requires a real success id, and 2 consecutive failures flip the tool's own result to "stop retrying, take a message". **Never call `updateTools()` inside a tool's own `execute()`** — it swaps out the tool LiveKit is waiting on ("function output missing"), the model retries forever, and the caller hangs up. Deferred to a macrotask. **Tree selection the model was TOLD to do, it does not reliably do — so the host does it from the ANSWER (2026-08-14, working tree).** The `meeting_topic` node's prompt said "when the answer names work for the owner… call `set_purpose` again with it"; on sim call `1786693849702` the caller said "This is about a job position", the model recorded the topic, booked 15 minutes, and wrote **zero** `job_inquiries` rows — purpose had already locked on "schedule a meeting" BEFORE the topic was known, so re-declaring it would have bounced anyway. `meetingTopicNamesOwnerRole()` now matches the role words (`position` / `role` / `recruiter` / `recruiting` / `staffing`, `job position|opportunity|offer|opening`, contract-to-hire, "hiring him/her/them/Dale/the owner") in `checklistTools.ts` and `tracker.select(['job'])` fires in host code. Deliberately NOT matched: bare "job" / "a job" — in this product "I have a job for you" is a SERVICE request, not a role offer. Same shape as the existing `meeting_offer` → `booking` escalation, and the same lesson as the goodbye gate: **a prompt sentence is a request, host code is a guarantee.**
  - **Live tenant config compiles into this runtime — it is not a second engine.** `tenants.checklist_preset_id` + `tenants.checklist_overrides` → `deriveChecklistRuntimeConfig` → `/agent-tools/tenant-config` `checklist_runtime_config` → `ChecklistAgent({ runtimeConfig })`. **FIVE presets (`law_firm_front_desk` added 2026-08-14 — small plaintiff-side firms; adds `case_intake`, the only tree whose intake ends in a human take-or-decline decision rather than a booking, and the default for `business_type` `law-firm`/`lawyer`/`attorney`/`legal`), and WHICH TREES A PRESET OFFERS IS THE THING THAT DECIDES WHAT A CALL CAN DO — a tree missing from the preset is unreachable no matter what the model asks for, because `ChecklistOverrides` can only SUBTRACT blocks (`disabled_conversation_blocks`) and has no way to add one.** `auto_shop_front_desk` / `salon_front_desk` / `local_service_front_desk` share the front-desk tree set on purpose; `owner_for_hire_front_desk` (added 2026-08-13) adds `job` for solo professionals whose line takes work offers, and is the default for `business_type` `owner-for-hire` / `answering-service`. **`job` was in `forbidden_trees` on all three original presets, so it was unreachable by EVERY tenant** — two recruiter calls on 2026-08-13 to a line whose greeting says "Dale is available for hire" wrote zero `job_inquiries` rows, and on the first the model asked for the tree by name and the host answered `No tree called "job"` (`CALL1.md`, `CALL2.md`). `fix_computer` is still reachable from no preset — deliberately parked until a call asks for it, and `presetCatalog.test.ts` now fails CI on any OTHER orphaned tree (`deliberatelyUnreachable` is a one-element allowlist; adding to it is the thing to argue about, not the CI failure). **Open ops action:** `scripts/pin-owner-for-hire-preset.sql` pins Thinking Hammer to the new preset and **Dale runs it AFTER the agent deploys** — an unrecognized `checklist_preset_id` falls back to the derived default, so running it early is a silent no-op that looks like it worked (`docs/planning/TODO.md`). Overrides: cannot disable `identity`; `booking_mode` / `message_mode`; `optional_node_ids` (listen-only, goodbye gate cannot stick); `required_node_ids` (`declined:true` refused, `finish_call` stays shut); `wording` on approved product questions only (identity `ask` stays platform-owned). Same field cannot be both required and optional. Business Settings → Call checklist includes a next-call dry-run (ASK / LISTEN / REQUIRED). Editing `tenants.system_prompt` / the LADDER still does nothing on a live call. See `docs/planning/ROADMAP.md`.
- `/src/services/scripts/blocks.ts` — **the LADDER. Dead on the live path; reachable only via `ENABLE_QUESTION_TREE=false`.** A tenant's `system_prompt` is composed from these blocks (`composeScript`, installed via `scripts/install-script.ts --file <business>.json`) and, under question trees, **is never passed to the model at all** — `ChecklistAgent` takes a ONE-LINE persona, and `index.ts` withholds the ladder on purpose (a 10.5k-char script buried the agent's actual job). Editing this file changes nothing about a real call today. It is kept, not deleted, because it is the rollback path if question trees are switched off. The lessons it encodes are NOT stale, they simply moved: IDENTITY → the `identity` tree, BOOK-first → the goodbye gate, INTAKE-varies-by-vertical → tree selection, COMPLETE → `isResolved()`, the `take_message` ELSE → the `message` + `generic_subject` trees. Intake blocks: `intake_job_inquiry` (staffing). (A speculative `intake_real_estate` block was removed 2026-07-16 — off-target vertical, zero tenants, never exercised: "build for real customers / test it or delete it" in action.)
- `/agent/src/toolPhases.ts` — **ladder-path only** (feeds `SpeakingAgent`'s `intakeTools`); question trees do their own tool selection per selected tree, so this file is not consulted on a live call. The lesson it holds is still true and is why `selectedTools()` exists: 25 tools on every turn is over every published ceiling (OpenAI: <20; LiveKit: 5-12) and the model hallucinated tool RESULTS. Under the ladder the model saw ONE PHASE at a time (intake ~15 / booking ~12 / manage ~9), with `start_booking` / `manage_appointment` as ROUTERS so the only path to the calendar ran THROUGH a tool call rather than the model talking its way there.
- `/agent/src/session/holdLines.ts` + `toolActivity.ts` — the runtime, NOT the model, covers dead air. The prompt used to tell the model to say "one moment while I look that up", which it could satisfy with a SENTENCE instead of a tool call — so it narrated lookups it never performed. The watchdog now speaks a PRE-SYNTHESIZED hold line from a TIMER, and it is honest: it may name the lookup only when a tool is genuinely in flight (`isToolRunning()`), otherwise it says "Just a moment", which promises nothing and so cannot be false. (It once said "let me check that for you" seven times on a call whose tools all returned in under 15ms; the caller asked, mid-call, what it was checking.)
- `/agent/src/session/dnsWarm.ts` + `dnsIpv4.ts` — **the pause a caller hears is not always TTS.** 2026-08-15, measured: a sim call greeted at `ms_since_participant: 11944` with `pregenerated: true` — cached frame, ready to play, and the caller still sat in twelve seconds of silence. It was ONE name lookup: `dns.lookup('api.deepgram.com')` = **11,069 ms** on the WSL dev host (getaddrinfo waits for A _and_ AAAA; the Windows-side resolver takes 11 s to answer AAAA — `dig AAAA … @1.1.1.1` does it in 46 ms). `dnsWarm` resolves the call-path hosts in **prewarm**, where the idle process pays instead of the caller, and logs any host over 1 s at WARN. `dnsIpv4` is the **default-OFF** (`DNS_FORCE_IPV4=true`) A-only shim for a resolver that stalls; it installs on `http/https.globalAgent.options.lookup`, **not** on `dns.lookup` — `node:net` captured its default lookup by reference at module load, so patching the dns module measured 18 ms and changed nothing (first TTS `collect()` stayed at 11.7 s; via the agents it dropped to 1.7 s). Local result: `ms_since_participant` 11,944 → **940 ms**. The real fix is the host's resolver; see root `DEVELOPMENT_WORKFLOW.md`. Related: `NUM_IDLE_PROCESSES` on `WorkerOptions` — the SDK keeps `min(cpus,4)` idle job processes in production and **0** in dev, so a locally-tested call pays process spawn + VAD load + every cold connection that production never does.
- `/supabase/migrations` — 196 SQL migrations (incl. 20260911010000 night_shift_morning_half (**the morning half of a night shift could never be booked.** Coverage was matched on the SLOT's own local date, so a 22:00→06:00 shift — whose `employee_schedule` row is dated the EVENING it starts — was never consulted for anything after local midnight: 23:00 booked fine, a 02:00 slot the next morning refused `EMPLOYEE_NOT_SCHEDULED`. New `shift_row_covers_booking()` wraps `shift_covers_booking()` (20260909120000) and adds the missing case — a row dated YESTERDAY covers TODAY's slot only when it is itself a wrapping night shift reaching that far, and today's slot doesn't also wrap into a third day. Six call sites in `book_with_scheduling_atomic` widened from `shift_date = v_shift_date` to `shift_date IN (v_shift_date, v_shift_date - 1)` plus the new function; `availabilitySearch.ts`'s suggest-side JOIN carries the identical fix in the same commit — suggest and enforce must read the same calendar (the 2026-07-17 midnight-wrap lesson). `book_appointment_atomic` (dashboard path) untouched — it has never modeled night shifts by design. Residual, deliberately out of scope: the `get_available_slots` DATE path in `src/routes/agentTools/scheduling.ts` has the identical same-date-only join and was not touched; no live tenant runs night shifts, so this is not an active gap) + 20260911000000 book_appointment_atomic_strict_links (the dashboard booking path caught up to the phone path's STRICT skill-map rule from 20260909210000 — `book_appointment_atomic` no longer falls back to `required_skills`/`required_resources` tags nor falls open when a service has no `service_employee`/`service_resource` links; it now refuses with the same wording the phone path already speaks. Same business, same service, two different answers depending on which door the booking came through — closed) + 20260909210000 booking_rules_past_and_person (**three rules the production booking RPC did not enforce.** (1) No booking in the past — `PAST_TIME`, one minute of grace; proved against prod by booking 1:00 PM at 6:52 PM the same day. (2) An appointment is WITH SOMEBODY — the fall-open that booked a room with `employee_id` NULL is gone, so a tenant with no scheduled staff cannot be booked (a rule change: `availability-search.test.ts` and the solo-operator fixtures were rewritten to it, not loosened). (3) **The skill map decides who and where** — when the service is known (`p_service_id`), only people linked in `service_employee` and rooms/lines linked in `service_resource` are used, the `employees.skills` / `services.required_skills` tags are NOT consulted, and a service with no active linked person or room is refused STRICT. A rolled-back probe had booked a meeting linked to Dale + the Zoom line with an unlinked employee in an unlinked storage closet. `availabilitySearch.ts` (`serviceId`), the available-slots date path (linked people's shifts only) and the appointments conflict suggestions carry the same rule — suggest and enforce read the same map. Guarded by `tests/regression/skillMapLinks.realdb.test.ts`, mutation-checked against the old function) + 20260909120000 shift_boundary_slack (**a shift boundary is fuzzy by ONE minute, and the rule now exists exactly once.** Callers name round times — "five o'clock" — and a shift saved as ending 16:59 refused the 17:00 they said, over a minute nobody meant. `shift_covers_booking()` takes a minute of slack at each edge; the un-slacked comparison is tried FIRST because `'23:59'::time + '1 minute'` wraps to `'00:00'` in Postgres and a naive widening would accept a slot a day out of place. It replaced FOUR hand-copied coverage clauses — three in `book_with_scheduling_atomic`, one in `availabilitySearch.ts` — and that duplication is not theoretical debt: suggest and enforce drifting apart IS the 2026-07-17 midnight-wrap call. Appointments still sit on the quarter-hour grid; the `appointments_start/end_time_15min` CHECKs are untouched, so no odd-minute appointment is ever written) + 20260901100000 starter_services (**the setup wizard asked a new owner what services they offer against a blank list.** `business_templates.example_services` was EMPTY for all 31 live business types, measured 2026-09-01. The column also changes text[] → jsonb, because names-only rows are near-useless to the thing that consumes them: `resolveServiceForBooking`'s semantic step embeds `concat_ws('. ', name, subtitle, description)`, so a look-first row seeded "Diagnostic visit" gives "my check engine light is on" almost nothing to match on — the DESCRIPTION does the retrieval work. A second column was rejected: this schema has been bitten three times by two lists that must agree. GENERATED FILE — author the content in `shared/starterServices.ts` and re-run `npm run starter-services:generate`; `npm run verify:starter-services` fails CI on drift, and `supabase/seed.sql` carries the same generated block so fresh rebuilds and existing databases agree. The type conversion is GUARDED so re-running is safe — unguarded, a second run dies on the text[] COALESCE once the column is already jsonb, and a migration that only works once is a migration that will strand somebody) + 20260903000000 blackout_dates (**`employee_schedule.is_off` says one PERSON is off; nothing could say the BUSINESS is shut.** Closing for a holiday meant editing every employee's row for that date, and anyone hired afterwards was silently bookable on a day the doors are locked — the failure lands on a customer standing outside. `blackout_dates (tenant_id, blackout_date, reason)`, natural composite PK, RLS enabled + forced. The booking RPC refuses a closed day with a new `BUSINESS_CLOSED` code checked BEFORE any employee/resource search — with the guard further down, a tenant with no staff that date would hear "no one is scheduled" instead of "we are closed": same facts, different sentence, only one of them true. `availabilitySearch.ts` carries the matching exclusion, because suggest and enforce must read the same calendar or the agent offers a slot the booking then refuses — the 2026-07-17 midnight-wrap lesson. `reason` is nullable and for the OWNER; the agent says "we are closed that day", not "staff wedding") + 20260821020000 drop_dead_crm_providers (two provider CHECK constraints — `entity_sync_map_provider_check` and `tenant_integration_settings_provider_check` — still enumerated `jobber`/`hubspot`/`servicetitan` alongside `square`, eleven weeks after those 21 dormant CRM adapters were deleted 2026-06-12. A constraint is a statement about what the system supports: leaving the dead three in it tells the next reader the product can sync Jobber, and nothing in the schema says otherwise. Both tables hold ZERO rows in prod, verified before narrowing, so no existing data could be rejected. Widening a CHECK is one line if a CRM ever returns — and it belongs with the adapter, not ahead of it) + 20260821010000 drop_inert_columns (`business_templates.voice_provider`/`voice_name` + `tenant_integration_settings.webhook_secret`, all with ZERO TypeScript readers. The two voice columns were not merely stale but FALSE: `cartesia`/`elevenlabs` with voice names `Josh`/`Rachel`, an ElevenLabs vocabulary for a product whose TTS has only ever been OpenAI then Deepgram Aura, and until #356 `GET /templates/full`'s `SELECT *` shipped them to every authenticated dashboard user. `tenant_integration_settings` holds zero rows in prod, so `webhook_secret` dropped nothing. The live per-tenant voice is `tenants.tts_voice`) + 20260821000000 drop_n8n_webhook (**an integration with no application surface fired on EVERY appointment INSERT.** `trigger_notify_n8n_appointment` called a SECURITY DEFINER `notify_n8n_on_appointment()` whose first act was a SELECT against `tenants` inside the booking transaction, to read a `tenants.n8n_webhook_url` that has ZERO readers and ZERO writers anywhere in the codebase — no route sets it, no dashboard field edits it, no type declares it. Verified against prod before dropping: 0 tenants held a value and `pg_net` was not installed, so nothing was lost. What made it worth removing rather than leaving inert is what it would have done if ever finished: with `pg_net` present the POST runs SYNCHRONOUSLY inside the booking transaction, so `book_with_scheduling_atomic` would block on an external host while holding the GiST exclusion constraints that make booking race-safe — a slow webhook endpoint becomes a booking outage. A webhook belongs in application code AFTER commit, like `syncOrchestrator`, where a failure is retryable) + 20260820000000 employee_schedule_pattern (**THE WIZARD HAD THE RULE AND THREW IT AWAY.** `employee_schedule` stores one dated row per shift and no rule, so `extendSchedules.ts` had to GUESS the weekly pattern back out of the rows — and it guessed from the last 7 days ending at `MAX(shift_date)` over ALL TIME. One one-off shift 300 days out ("annual inventory Saturday") makes that tail week Saturday-only: Mon–Fri quietly stop being extended and the business goes unbookable in ~180 days, killed by the worker written to prevent exactly that. Every row-archaeology alternative is wrong in its own way — a recent-window rule resurrects a weekday the owner dropped, a "densest week" rule over-schedules the light leg of a rotation (worse than under-scheduling: it puts a real customer in front of a locked door), a "3+ weekdays" rule never qualifies for the Saturday-only owner and leaves them unbookable. `employee_schedule_pattern (tenant_id, employee_id, day_of_week)` — natural composite PK, no surrogate — stores the DECLARED rule, written by `expandWeeklyToSchedule` from the same weekly grid the wizard already collects, and REPLACED not merged (a weekday absent from the pattern means the owner dropped it; merging would resurrect it forever). `extendSchedules` projects the declared rule where one exists and falls back to the derived tail week where none does — **and that fallback's tail is now clamped to `CURRENT_DATE + 14`**, which is the bug fix for every tenant that predates the table. NO BACKFILL, deliberately: inventing a rule from existing rows is the archaeology the table exists to end; legacy tenants get the clamped fallback until they next save their hours) + 20260819000000 reminder_schedules_sending_status (**THE ENTIRE REMINDER PIPELINE WAS DEAD FOR 13 DAYS AND NOTHING SAID SO.** #322 (2026-08-06) added an atomic claim to the reminder worker — `UPDATE reminder_schedules SET status='sending' ... FOR UPDATE SKIP LOCKED` — to stop a double-text on every Railway deploy. `reminder_schedules_status_check` allowed only scheduled|sent|failed|cancelled, so the claim threw `violates check constraint` on EVERY 60s tick; the exception landed in `processBatch`'s outer catch, bumped `errors_total{event="reminder_batch_failed"}` on the token-gated `/metrics` nothing scrapes, and returned 0. Worker alive, `/health` green, zero reminders sent. Every existing test mocked the pool and a mock has no CHECK constraints — which is why `tests/regression/reminderClaimRealDb.test.ts` is real-DB and runs the worker's claim statement verbatim. No backfill was needed and that is the only luck in it: the constraint REJECTED the write, so no row was ever left corrupt — the outage was total, not partial. Two more pieces ship with it, and all three must move together or reminders break again: `processReminder` now accepts `'sending'` (it gated on `!== 'scheduled'`, so the instant the claim worked it would have silently skipped every claimed row and stranded it where the claim query — which only selects `'scheduled'` — never looks again), and `releaseStaleClaims()` returns rows abandoned in `'sending'` past 5 min to the queue (a claim is a way to LOSE a reminder that `'scheduled'` never had: SIGTERM past the 10s drain, OOM, eviction). `triggerReminder` deliberately still refuses `'sending'` — the human send-now path must not race a live worker) + 20260814130000 question*trees_per_tenant (**THE QUESTIONS BECAME DATA IN THE DATABASE.** `question_tree_templates` / `question_tree_template_nodes` hold a generic per-VERTICAL starting point; `tenant_question_trees` / `tenant_question_nodes` hold each client's OWN copy, made by `copy_question_tree_templates_to_tenant()` inside the same transaction that creates the business. Editing a client's rows changes their calls with no deploy and touches nobody else. Nesting is carried by `parent*\*\_node_id`+`option_key`+`sort_order`, NOT by node_id: a node id legitimately repeats across branches (`job.rate_range`under both`contract`and`contract_to_hire`), so uniqueness is per BRANCH — a first cut keyed on (tree, node_id) was rejected by the real library on the first seed. Copy idempotency is at TREE granularity so a re-run can never restore a question a client deleted. The TS library is NOT dead: it is the template CONTENT (projected by `scripts/seed-question-tree-templates.ts`) and the runtime FALLBACK — a tenant with no rows behaves exactly as before, so shipping this changes no existing call) + 20260814120000 checklist_preset_id_catalog_sync (**the preset CHECK constraint had silently fallen a preset behind.** `owner_for_hire_front_desk`shipped in code 2026-08-13 and was never added to the enum, so`scripts/pin-owner-for-hire-preset.sql`— the ops step HANDOFF tells Dale to run against prod — aborts with`violates check constraint "tenants_checklist_preset_id_valid"`. Verified by running that UPDATE against a real row. Adds `owner_for_hire_front_desk`+`law_firm_front_desk`; `tests/presetCatalogConstraint.test.ts` now fails CI on the next drift) + 20260813000000 tenant_checklist_overrides (`tenants.checklist_overrides`jsonb NOT NULL DEFAULT`{}`— the SUBTRACT-only override envelope:`disabled_conversation_blocks`/`booking_mode`/`message_mode`/`optional_node_ids`/`required_node_ids`. Invalid entries are ignored on read and rejected on write. It has no ADD verb by design, which is exactly why a tree missing from the preset is unreachable — see the job-tree outage below) + 20260812000100 tenant_checklist_preset_id (`tenants.checklist_preset_id`TEXT + a CHECK constraint enumerating the supported preset ids —`business_type`still SEEDS the default preset, but an owner can now pick a different one without forking the rest of tenant config. An UNRECOGNIZED id falls back to the derived default, so pinning a preset before the agent that knows it deploys is a SILENT no-op) + 20260811160000 intake_submissions (generic type-tagged intake envelope — one row per structured capture, payload-preserving, reusable across projections.`capture-job-inquiry`is the first writer and inserts HERE FIRST, before the specialized`job_inquiries`row, so the generic capture survives however the projections later fork. The point is that the next vertical's structured capture does not need a brand-new top-level table) + 20260802000000 schema_migrations_no_rls (prod had RLS ENABLED with ZERO policies on the migration bookkeeping table — deny-all for any role that cannot bypass, which`app_user`now is. Nobody hit it because migrations were being applied over the old superuser URL, but under the repointed`DATABASE_URL` the runner would have read an EMPTY schema_migrations and tried to re-apply all 178. The table has no tenant dimension, so RLS on it protects nothing; a permissive allow-all policy would have been worse — a control that looks like a control and enforces nothing) + 20260801020000 customer_messages_urgency (`is_urgent`+ a partial index on unhandled urgent messages — a caller who said "I want to talk with him URGENTLY" was answered with a list of appointment slots and hung up mid-sentence. There is no live-transfer path on the question-tree flow, so the honest escalation is to take the message and MARK it; the flag is set from the caller's own words, never inferred from topic, and a later correction can only ever RAISE it) + 20260801010000 customer_messages_updated_at_honest (backfills`updated_at = created_at`and adds the house`fn_set_updated_at`trigger — the previous migration's`ADD COLUMN ... NOT NULL DEFAULT now()`stamped EVERY existing row with the migration's own timestamp, so untouched history read as "revised", the exact opposite of the column's purpose; and the dashboard's status UPDATE never bumped it, so it was reliable for corrections and quietly wrong for everything else. A column maintained at one call site lies at all the others) + 20260801000000 customer_messages_one_per_call (partial UNIQUE on`(tenant_id, call_id)`WHERE call_id IS NOT NULL +`updated_at`— take-message was INSERT-only, so a mid-call CORRECTION had no row to reach: "Jamil" was saved, the caller said "no — Camille, C-A-M-I-L-L-E" thirty seconds later, and the row still says Jamil in prod. The route now UPSERTs, which fixes the correction AND makes the tool retry-safe — the same one-row-per-call shape as job_inquiries 20260717230000) + 20260731000000 tenant_booking_mechanics (nullable`tenants.booking_mechanics` — WHAT HAPPENS at the booked time, in the owner's words, spoken verbatim after every successful booking. "Booked a call" never defined who dials whom, so on 2026-07-27 the agent confabulated the agreeable answer — "call Dale directly at two thirty… you can use the same number", which is the AI receptionist's OWN line — and that single sentence produced four more failed calls from the same caller. NULL/blank = the confirmation says nothing extra, which is honest; guessing is not) + 20260730120000 job_inquiry_role_description (`job_inquiries.role_description`TEXT — the job tree always COLLECTED the role in the caller's own words and the write dropped it end-to-end: no tool param, no Zod field, no column. Verified on prod call SCL_nRKo3KEVw8Yh 2026-07-30 — every other field landed; WHAT JOB it was survived only in the transcript. Now: column + Zod + tool param + ACTION_ARG_BACKFILL + owner-email "Role" row + it leads the appointment job-summary stamp, with`captureCompleteness.test.ts`asserting every collected job-call node maps to a capture param or a declared control node — the next silent drop is a CI failure) + 20260722000000 templates_list_core_lanes (data-only: rewrites every`business_templates.first_message`to a generic core-lanes invite — book / message / ask about services — so a NEW tenant's greeting lists what the business can do before the caller speaks; existing tenants keep their own`first_message`; owners customize on Phone Assistant → AI Persona) + 20260721130000 end_voice_session_recaptures_context (`end_voice_session`re-runs`get_customer_context_for_call`at CALL END and overwrites the stored`voice_sessions.customer_context`— the snapshot was written by`start_voice_session()`at connect time, i.e. the caller's history BEFORE the call, so a first booking showed "Booked / 0 appointments" and the number was useless for review. Now reflects what the call produced: first booking → 1, the 0→2 double-booking → 2. Only fires when the call has a customer_id) + 20260721120000 tenant_greeting_menu (nullable`tenants.greeting_menu`— owner-editable spoken services menu, said between the AI disclosure and the closing question so callers hear the CORE lanes up front, incl. that the AI secretary itself is for sale; NULL/blank = greeting unchanged) + 20260718003000 booking_rpc_midnight_wrap (wrap-aware shift-coverage end in`book_with_scheduling_atomic`— a slot ending at LOCAL MIDNIGHT casts to 00:00 and compared as "before" any afternoon shift end, so an 11:30 PM slot was offered AND booked against a 1–5 PM shift on a live call; '24:00:00'::time when the slot's local end date passes the shift date. Plus a narrow guard on the skill-less path: schedule rows on the date + none covering → EMPLOYEE_NOT_SCHEDULED; tenants with no schedule data keep the historical fall-open. availabilitySearch.ts carries the identical suggest-side fix) + 20260717230000 job_inquiries_one_per_call (partial UNIQUE on`(tenant_id, call_id)` WHERE call_id IS NOT NULL — an ACTION-rung tool is retried until it returns its success id, so concurrent retries of one call must converge on ONE row; four in-flight retries behind a hung SMTP send wrote four identical inquiries on 2026-07-17. Route pairs it with INSERT … ON CONFLICT DO NOTHING + winner lookup; rung recipe rule 12) + 20260716120000 job_inquiry_appointment_link (`job_inquiries.appointment_id`FK → appointments, ON DELETE SET NULL — a "meeting about a job" call now links the inquiry to THE meeting it booked; the capture route verifies the id belongs to the tenant, saves the inquiry UNLINKED on a miss (the row is the lead, the link is context), and stamps a`Job details: …`summary into the appointment's description. Sibling route`/agent-tools/attach-meeting-notes`appends`Caller notes: …` from the meeting-goals rung's one wrap-up question. The appointment id is injected by the AGENT RUNTIME from the call-outcome tracker — the model never holds a UUID) + 20260714150000 service_intent_embeddings (`services.embedding vector(1536)`+`match_service_by_intent()`— **the model no longer PICKS the service**.`resolveServiceForBooking`'s ILIKE step can only fire if handed a string containing the service's NAME, and callers never say one — so the LLM guessed a catalog name and the ILIKE dutifully matched its guess. A caller wanting a meeting about a six-month contract got booked into a 15-minute "Personal Callback": the wrong answer arrived through a working mechanism. Choosing from a catalog is RETRIEVAL, not reasoning. The agent now passes the CALLER'S OWN WORDS and pgvector finds the nearest service by MEANING — the DESCRIPTION does the work, since nothing in the NAME would ever pull "talk to him about a position" onto "Programming Consultation". Embeddings are lazy + self-healing; a low-confidence match falls through to the tenant default, because a BAD semantic match books the wrong thing silently) + 20260714140000 booking_rpc_records_the_service (`book_with_scheduling_atomic`— THE production booking path — had an INSERT INTO appointments that did not list`service_id`, and never had. **Every appointment ever booked by phone had a NULL service**: the owner opens his calendar and sees a customer, a time, and a blank. The function already TOOK the service and used it only to pick a skilled employee, then threw it away. NB adding a param creates a second OVERLOAD; the old one must be dropped or every booking dies "function is not unique") + 20260714130000 job_inquiry_two_companies (a staffing call has TWO companies — the AGENCY that rang, and the CLIENT where the work is. They were collapsed into one, so the owner knew where the work was and had no idea who had called him) + 20260714120000 sync_customer_names_on_insert (the name-splitting trigger was BEFORE UPDATE only, and `identify_caller`upserts — so every customer the VOICE AGENT ever created had a NULL first_name/last_name. The dashboard looked right because it renders`name`; the CSV export of a caller the agent captured had no name in it) + 20260714000000 phone_verification_call_binding (`phone_verifications.call_id`— a proof of possession is bound to the CALL it was proved on. The disclosure gate used to accept any row verified for`(tenant, phone)` in the last 24 HOURS, so ONE legitimate verification opened a day-long window in which ANY caller who spoke that number was handed the account — the gate degraded into "was this number ever verified recently", which is the claim-based trust it exists to destroy. A code proves you held the handset at a MOMENT, not that the number is yours until tomorrow. NULL call_id can never satisfy the gate — an unattributable proof is not a proof. Cost, accepted: on a forwarded line a returning customer verifies on every call; on a normal line the carrier attests the number and no OTP is ever needed, so the cost falls only where it must) + 20260713000000 tenants_soft_delete (`is_deleted`/`deleted_at`/`deleted_by`on`tenants`— the app NEVER hard-deletes a tenant now:`DELETE /tenants/:id`and the 60s demo-expiry reaper both flip the flag instead. A hard DELETE cascades away every appointment, customer, call recording and consent record, AND deadlocks against fire-and-forget reminder seeding. Destruction is now a manual maintenance act:`scripts/purge-soft-deleted.ts`, which dry-runs by default and prints the blast radius first. The is_deleted filter is enforced at a CHOKE POINT — `createWithTenantClient`404s a soft-deleted tenant — plus six enumerated raw-pool paths, the nastiest being`getDueReminders()`: soft-delete does NOT cascade, so without it a deleted business would keep TEXTING ITS CUSTOMERS) + 20260712010000 reminder_lead_minutes (`reminder_schedules.lead_minutes`INT NOT NULL — the lead is DATA now, not something you parse back out of the`reminder_type`string;`reminder_type`CHECK widened with`'custom'`for the caller-chosen reminder the voice agent offers at booking) + 20260712000000 customer_preferences (preferences moved OFF the`customers.metadata`jsonb blob into their own table, PK`(customer_id, pref_key)`, unbounded TEXT value, `updated_at`on re-save;`get_customer_context_for_call`redefined to aggregate from it — the wire shape the LLM sees is unchanged) + 20260711000000 tenant_call_disclosure (nullable`call_disclosure`+`\_attested_at`/`\_attested_by`on`tenants`— owner-editable spoken caller disclosure; NULL/blank = platform default from`agent/src/greeting.ts`, custom value is attestation-gated on write) + 20260702000000 cancel_orphaned_appointments_of_deleted_customers (data-only backfill: cancels upcoming 'scheduled' appointments of already-soft-deleted customers — the pre-fix orphans) + 20260701020000 reminder_schedules_unique_scheduled_bundle (partial unique index: at most one 'scheduled' reminder per appointment+type; seed INSERT is ON CONFLICT DO NOTHING — DB-level idempotency, no advisory locks/transactions which deadlocked the appointments cascade) + 20260701010000 fix_version_rpc_pk_names (restore_fields_from_version + copy_fields_between_records made PK-aware + jsonb type-correct decode; they'd been dead since the 2026-05 PK renames) + 20260701 booking_rpc_filter_soft_deleted + 20260630 tenant_persona_name + 20260630 default_service_and_skill_backfill + 20260629 tenant_forwarded_from_phone + 20260625 job_inquiries + reap_stale_voice_sessions + 20260624 voice_session_caller_phone_nullable + 20260622 voice_session_requested_service + audit_services_employees + 20260619 fix_rls_gaps + 20260618 ai_cost_events + ai_cost_events_rls + 20260616 customer_messages + 20260615 preferences_default_true + 20260612 knowledge_suggestion + comms history + voice styles + forward phone + 20260607 tenants_default_buffer + booking_buffer_enforcement). **`supabase/baseline.sql`is a generated pg_dump snapshot — regenerate with`npm run db:baseline` after any schema migration\*\* (the schema-alignment guard fails CI when a migration-created table/column is missing from it).
- `/scripts` — `simulate.sh` (system simulation/health harness; subcommands: `status`, `ci`, `tools`, `stripe`, `rag`, `toolselect`, `call`, and `all` = status + tools + rag; node helpers in `agent/scripts/sim-*.mjs` for LiveKit + `agent/scripts/sim-toolselect.ts` (LLM tool-selection eval, tsx) + `scripts/sim-tools.mjs` + `scripts/sim-stripe.mjs`), `verify-claude-md.ts` drift detector (counts route modules / SQL migrations / YAML bundles against the live filesystem, checks every `- \`/path\``in Key Directories exists, and checks every 7–40-hex string in backticks is an ancestor of`main`— mark a deliberately unmerged hash with a`<!-- verify-claude-md: unmerged -->` comment or it re-flags forever)

## Development

**Primary reference for this project**: See `docs/workflow/DEVELOPMENT_WORKFLOW.md`.

**For a reusable, project-agnostic version** that can be copied and adapted to other codebases, see:

- `docs/workflow/PORTABLE_DEVELOPMENT_WORKFLOW.md`
- `workflow.config.json` (repo root)

These two files are designed so another project can read them and implement the exact same development, testing, documentation, and commit processes.

Quick commands:

- Bootstrap: `npm run bootstrap` (deps + DB + migrations + seed + tests)
- Migrate: `npm run db:migrate [-- "postgres://..."]`
- Seed: `npm run db:seed [-- "postgres://..."]`
- Rebuild from scratch: `npm run db:rebuild [-- --yes]` (DROP SCHEMA public + apply all migrations + seed). End-to-end validation of the migration chain. Refuses non-localhost URLs unless `--force`; refuses without confirmation unless `--yes`.
- Start: `npm start` (Dashboard https://localhost:4000, Backend https://localhost:4001)
- Test: `npm test` (backend), `cd dashboard && npm test`, `cd dashboard && npx playwright test` (e2e)
- Simulate / health-check any time: `./scripts/simulate.sh status --env prod|local [--deep]` (HTTP board for backend/dashboard/agent; `--deep` dispatch-tests the LiveKit agent worker; also reports build staleness), `npm run status` (same as above), `./scripts/simulate.sh ci [--watch]` (GitHub Actions CI job stages + conclusions for the 4 CI jobs + local build freshness / src-vs-dist delta), `npm run ci:status`, `npm run ci:watch`. `./scripts/simulate.sh tools [--env local] [--tenant <id>]` (realistic agent-tools journey — provisions an ephemeral `/demo/start` tenant, books, recalls a preference, maps unwired `[dev]` links as GAPs), `./scripts/simulate.sh rag [--env local]` (RAG accuracy eval — seeds a known KB, asks paraphrased questions via `/agent-tools/policy-answer`, reports a retrieval hit-rate; real OpenAI embeddings, on-demand not CI), `./scripts/simulate.sh call --tenant <id>` (dispatch agent + print a browser join URL to talk to it with a mic, no phone). `./scripts/simulate.sh toolselect` (LLM tool-selection eval — replays the real system prompt + real tool schemas through the agent model (override via `SIM_TOOLSELECT_MODEL`; load a tenant ladder via `SIM_CUSTOM_PROMPT_FILE` — script-ordered cases are skipped without one), feeds synthetic tool results, grades the chosen tool sequence, e.g. `get_available_slots` must lead to `book_with_scheduling` never `book_appointment`; real OpenAI calls, on-demand not CI). Tiers: status=systems up · ci=CI+build state · tools=brain works · toolselect=LLM picks right tools · stripe=billing wired · rag=answers accurate · call=voice works. Only real PSTN inbound can't be simulated.
- **Local voice calls: `cd agent && npm run dev:local`, NOT `npm run dev`.** It sets four things that were each a real local-only defect (2026-08-15): `AGENT_NAME=secretary-hq-agent-dev` (the default name is the SAME one Railway registers under, and LiveKit load-balances the dispatch across both — a coin flip with prod), `NODE_EXTRA_CA_CERTS=../certs/localhost-cert.pem` (the backend is HTTPS with a self-signed cert, so the agent's `fetch` to `BACKEND_URL` fails TLS: `voice_session_start_failed` and a tenant config silently degraded to the name "this business"), `NUM_IDLE_PROCESSES=1` (the SDK keeps **0** idle job processes in dev, so the caller pays process spawn + VAD load), and `DNS_FORCE_IPV4=true` (see `/agent/src/session/dnsIpv4.ts`). Per-tenant question trees on a local DB: `npm run trees:local` (seeds templates + converts every tenant) — **Playwright's globalSetup rebuilds the DB, so re-run it after an e2e run**; without rows the agent falls back to the TS library and calls still work. The number to read afterwards is `greeting_spoken … ms_since_participant` in the worker log: under a second is healthy.
- Quality gates: `npm run checks` (format + lint + typecheck), `npm run pre-pr`
- Heavy pre-commit automation: `npm run prepare-commit` (runs checks + tests + drift detector + more)
- Create feature branch (recommended): `npm run create-branch feat/my-work` or `bash scripts/create-feature-branch.sh feat/my-work`
- Verify docs drift: `npm run verify:claude-md`. Verify the baseline snapshot still matches the migrations: `npm run verify:schema` (this is the guard that fails CI when a migration-created table/column never made it into `supabase/baseline.sql` — run `npm run db:baseline` after any schema migration). `npm run generate-kit` regenerates `portable-workflow-kit/`.
- **Verify the agent can actually SPEAK: `cd agent && npm run verify:tts`** — opens the REAL Deepgram WebSocket with the REAL config and demands real audio bytes back, for every voice the picker can map to. **MANDATORY before any TTS change reaches prod.** Origin: 2026-07-14 — a TTS engine swap passed typecheck and all 567 unit tests and took the phone line COMPLETELY SILENT (the plugin appends `?speed=…` to the WS upgrade URL; Aura answers 400; the socket never opens; there is no TTS at all). Not one of those tests synthesises a word — they all mock the TTS. **"It compiles and the tests are green" is not the same as "it makes noise."** `tenants.tts_speed` is consequently INERT under Aura — it is not passed, because passing it is what caused the outage.

Logins (all `/ password`):

- `admin@secretaryhq.com` — platform super-admin on tenant `00000000-0000-0000-0000-000000000000`
- `daledemott@gmail.com` — Thinking Hammer LLC owner on tenant `d5e3c6a1-7b9f-4e2a-bf30-8c11a5d8e9f0` (Dale's real business; intentionally separate from his super-admin identity so platform rights don't bleed into business workspace)
- `bella@bellashair.com` — Bella's Hair Studio owner on tenant `b3e1aaaa-bbbb-4ccc-8ddd-eeeeeeeeeeee` (salon demo tenant; DynaTire PoC was removed 2026-06-03 after the customer declined the service)
- Docker DB on port 5433

## Database Key Details

- **✅ RLS IS ENFORCED IN PRODUCTION (verified 2026-08-02).** Production connects as `app_user` (`rolsuper=f`, `rolbypassrls=f`), so the policies are a real second layer rather than decoration, and tenant isolation no longer rests solely on `tenantMiddleware`. `GET /ready` reports `rls_enforced: true` + `db_role: app_user` from the running process's OWN connection — observed, not assumed. Probed directly against prod 2026-08-02: **38 of 38** tables with RLS enabled, **52** policies, **0** reading `current_setting()` raw, and `app_user` unable to bypass. The three migrations that got here: `20260724000000_rls_null_safe_context.sql` (rewrote **49** policies onto `tenant_ctx()` / `tenant_ctx_uuid()`), `20260724000050_rls_close_gaps.sql` (`message_delivery_status` — RLS enabled + forced + 2 policies), `20260724000100_app_user_role.sql` (creates the role; its password is a 32-char random set by `ALTER ROLE` in the same transaction — the placeholder in the file is local/CI only). **Landmine 1 (silent empty results):** `admin_bypass` policies tested `current_setting(...) = ''`, but on a cold pool connection that GUC is NULL and `NULL = ''` is NULL, so the policy DENIED — `getDueReminders()` returned zero rows and every reminder would have stopped silently. **Landmine 2:** most policies cast the GUC straight to uuid while `clearTenantContext()` sets it to the EMPTY STRING on pool release, so `''::uuid` raised `invalid input syntax for type uuid: ""` — a 500 per request on every table. Both are fixed and were probed on prod. **HOW TO VERIFY, and how NOT to:** `SET ROLE app_user` from a `postgres` session is REFUSED, so a probe run that way silently executes as `postgres` — which bypasses RLS — and its "cross-tenant rows" prove nothing (this happened on 2026-08-02 and the numbers were briefly misread as a leak). A real isolation probe needs a genuine `app_user` connection; `tests/regression/rlsIsolation.test.ts` is that probe and runs in CI. **Reverting is one env var** — keep the old superuser URL to hand. **NB migrations against prod:** `npm run db:migrate` writes `schema_migrations`; migration `20260802000000` disables RLS on that bookkeeping table precisely so the runner works under `app_user` (before it, an app_user run would read an EMPTY migrations table and try to re-apply everything). **NB `rebuild-db.sh` re-applies the app_user migration after a baseline restore** — `pg_dump` never dumps roles and `baseline.sql` is dumped `--no-privileges`, so without that step a rebuilt DB fails as `permission denied for table customers`, which reads like an RLS bug and is not one.
- RLS via `app.current_tenant_id` context var. `FORCE ROW LEVEL SECURITY` is declared on every table that holds tenant data, and is LIVE (see above). `schema_migrations` is deliberately exempt — it has no tenant dimension, and RLS on it broke the migration runner under `app_user` (migration 20260802000000).
- Single DB pool via `DATABASE_URL` (Supabase managed; no separate `api_user` pool).
- Admin bypass policies on `tenants`, `users`, `business_templates` for cross-tenant ops with no tenant context.
- Audit trigger `fn_audit_trigger` is `SECURITY DEFINER` to bypass RLS for internal logging.
- **`employee_schedule` is the single source of truth** for shifts: `(tenant_id, employee_id, shift_date, start_time, end_time, is_off)`. The earlier weekly-pattern `employee_shifts` table was dropped 2026-04-30. Setup wizard collects a weekly grid in form state and posts it to `POST /shifts/expand-weekly` (`expandWeeklyToSchedule()` in `src/services/expandWeeklyToSchedule.ts`) — fans the pattern into `employee_schedule` for 4 weeks. Owners extend forward via the Schedule tab's copy-week button. API: `GET/POST /shifts/overrides`.
- **Booking RPCs** (both read `employee_schedule` directly):
  - `book_appointment_atomic()` — 7-layer constraint check + past-time rejection + employee-shift coverage (rejects with `EMPLOYEE_NOT_SCHEDULED` when no shift in `employee_schedule` covers the requested time) + service-aware skill+resource enforcement (when `p_service_id` is provided, prefers `service_employee` / `service_resource` mapping tables as the authoritative gate; falls back to `services.required_skills` / `required_resources` array check only when the mapping is empty for that service) + fuzzy service match. There is no separate tenant-level "business hours" config — the building's open window is implicitly the union of staff shifts.
  - `book_with_scheduling_atomic()` — production booking RPC; date-based shift validation, cross-midnight night shifts, specific error codes (`TIMESLOT_OCCUPIED`, `NO_SKILLED_EMPLOYEE`, `EMPLOYEE_NOT_SCHEDULED`, `NO_AVAILABILITY`, `INVALID_PARAMS`, `PAST_TIME`, `BUSINESS_CLOSED`). Since 20260909210000: refuses a past start, never writes an appointment with no person, and **when `p_service_id` is given the skill map's `service_employee` / `service_resource` links are the whole rule** (tags ignored, unlinked service refused). Without a service id the legacy tag rules apply. NB the dashboard path (`book_appointment_atomic`) still FALLS BACK to tags when a service has no links — the two paths differ on an unlinked service. Known gap, pre-existing on `main`: coverage is matched on the slot's own date, so the morning half of a night shift is never bookable (`docs/planning/TODO.md`).
  - Both protected by GiST exclusion constraints (`appointments_no_resource_overlap`, `appointments_no_employee_overlap`) — race-safe under READ COMMITTED.
- `get_effective_shifts()` / `get_effective_shifts_bulk()` — return rows from `employee_schedule`. Bulk variant powers the scheduler.
- `check_availability_with_tz()` — timezone-aware availability lookup
- `check_coverage_gaps()` — coverage analysis powering the dashboard bars
- `search_tenant_docs()` — cosine similarity over pgvector embeddings
- **ID convention** — domain entity tables (`tenants`, `customers`, `appointments`, `employees`, `resources`, `services`, `skills`, `users`, `voice_sessions`, `record_versions`) use `UUID PRIMARY KEY DEFAULT gen_random_uuid()`. Append-only audit / event tables (`reminder_schedules`, `consent_records`, `opt_out_records`, similar) use `SERIAL PRIMARY KEY` for the row's own id, but `UUID` for every FK column pointing at a domain entity. Mental rule: if an id is referenced from outside its own table, it's UUID; if it's only ever an internal sequence number, it's SERIAL. TS types must match — `string` for UUID columns, `number` for SERIAL. Polymorphic assignment uses `p_assignment_id` UUID. Origin of the rule: 2026-05-11 found `ReminderSchedule.appointment_id` typed `number` against a UUID FK column — every INSERT would have crashed against real Postgres, but 24 mocked unit tests hid it. This class of type/DB shape drift continues to be actively hunted (e.g. May 2026 removal of a stale duplicate `ReminderSchedule` definition inside `src/services/reminders/types.ts` that was missing the retry columns; and normalization of the last camelCase holdout `OptOutRecord` to snake_case).
- **PK column-name convention** — every single-column PK is named `<table_singular>_id`, never bare `id`. So `customers.customer_id`, `appointments.appointment_id`, `reminder_schedules.reminder_schedule_id`. The benefit is JOIN symmetry: `appointments.customer_id = customers.customer_id` lets you write `JOIN customers USING (customer_id)`, and a `SELECT *` across joined tables produces unambiguous column names (no aliasing needed). Sub-rules: (a) junction tables (`service_employee`, `service_resource`) keep composite PKs `(left_id, right_id)` — no surrogate `<junction>_id` added; the composite IS the identity. (a2) 1:1 extension tables (`tenant_calendar_settings`, `appointment_sync_map`) reuse the parent's PK as their own (`<parent>_id UUID PRIMARY KEY REFERENCES <parent>(<parent>_id) ON DELETE CASCADE`) — same "relationship IS the identity" reasoning, and the PK-as-FK enforces "at most one row per parent" at the PK level. (b) When a FK is _role-based_ and can't share its target's PK name (e.g. `audit_log.created_by_user_id` vs `audit_log.edited_by_user_id` both pointing at `users.user_id`), the FK is named `<role>_<table>_id`, keeping the `_<table>_id` suffix so the column name still tells you the referenced table. Symmetric `USING` is available only on the unambiguous columns; the role-based ones use explicit `ON`. (c) Abbreviations are forbidden — `voice_session_id` not `vs_id`, `reminder_schedule_id` not `rs_id`. Self-derivability beats brevity. Origin: 2026-05-11 conversation locking the standard down after the reminder-bug surface area review surfaced the asymmetric `<table>.id` vs `<other>.<table>_id` shape across the schema. Migrations to apply this rule land table-by-table as pilot work proves the pattern; each migration is `ALTER TABLE <name> RENAME COLUMN id TO <name_singular>_id`.
- **In-memory / non-DB identifier convention (2026-05-27 addition)** — The same bias toward descriptive `_id` names applies to TypeScript shapes that represent entity-like objects even when they are not direct DB rows (e.g. `CustomerNote` stored inside `customers.metadata.notes` JSON, lightweight DTOs, API responses that look like rows). Use `note_id`, `document_id`, etc. **Carve-out**: bare `id` is acceptable only for purely local/ephemeral UI state that is never persisted, never sent over the wire as a primary key, and has no meaning outside a single render (tab identifiers like `'schedule' | 'customers'`, theme keys, wizard step tokens, local `Map` keys for answers, etc.). When in doubt, prefer the `_id` form. This was the last open item from the great 2026-05 PK/naming retrofit (history in `docs/planning/RESOLVED.md`). The concrete pilot was `CustomerNote.id → note_id` (one interface + one React `key=` usage; typechecks + greps clean).
- **Composite / natural keys preferred when the natural key is short and stable.** When designing a table — new or being retrofitted — ask first: "what would make a row natural-keyed?" If the answer is 1-2 stable columns (e.g. `(tenant_id, slug)` for `business_templates`, `(service_id, employee_id)` for junctions), use that composite as the PK directly — no surrogate UUID. The key carries meaning the surrogate doesn't: it tells the reader what makes a row unique, and the schema enforces it at the PK level rather than a separate UNIQUE constraint. Use a surrogate `<table>_id UUID` only when (a) the natural key is 3+ columns, (b) any part of it is mutable, or (c) the row needs a portable identifier in URLs / external systems. **Retrofit cadence:** one table per day, same pilot pattern as the 2026-04→05 PK rename. Each retrofit lands as its own migration (drop surrogate, switch PK to composite, rewrite every FK, update TS types, run full e2e), CI-green per commit before moving to the next. Origin: 2026-05-18 — Dale's reflection that scattered surrogate UUIDs across the schema lost the natural-uniqueness intent. Every NEW table from this point on must justify its surrogate UUID before adding one; every EXISTING surrogate-PK table is on the retrofit queue.

## Build Principles

Durable rules-of-engagement that override "build for the future":

- **Debugging discipline (full log: `docs/workflow/LESSONS_LEARNED.md`).** When stuck: (1) **MEASURE before fixing** a latency/perf problem — one direct timing test beats hours of guessing (the voice "freeze" was OpenAI TTS at 2–5s, not the LLM/turn-detection/etc. I guessed). (2) **Don't blind-patch** — a wrong fix costs a full CI+deploy+retest cycle and the user's next test. (3) **Get the real trace first**, then fix. (4) **Instrument every sad path** (esp. fire-and-forget) with a **metric** (survives log truncation) + a 5W log naming the cause. (5) Observability that bypasses a throttled Railway API: the app's own `/metrics`, the prod **DB directly**, `railway logs`, Sentry/Better Stack. Append new lessons to `docs/workflow/LESSONS_LEARNED.md`.
- **Test it or delete it.** Code that can't be exercised against a real external surface (real CRM, real provider API, real billing event) doesn't ship. Mocked-API tests prove the mock works, not the integration. Origin: deleted 21 dormant CRM adapters 2026-05-02 (none had ever touched a real CRM; two were HIPAA-vertical violations).
- **Build for real customers, not the imagined Pro tier.** No provider integrations, billing tiers, dashboard sections, or service layers because we _might_ need them. Wait for a beta customer or sales call that names the need.
- **Working flat code beats a dormant abstraction.** When a "shared interface" or "registry" exists alongside the working flat-file equivalent, the flat files are the source of truth. Extract a shared shape after the third or fourth real consumer asks for it.
- **HIPAA verticals are permanently excluded.** Medical, dental, chiropractic, optometry, veterinary. Anything that surfaces them gets deleted on sight.
- **DB starts bare-bones, tests own their data, DB ends bare-bones.** The seed (`supabase/seed.sql`) seeds ONLY what an empty business needs to exist: tenant rows + owner users + business templates. **No business shape at all** — no services, employees, resources, or shifts (this doc falsely claimed Bella's business-shape was seeded until 2026-07-17; seed.sql's own sections 2–9 are "intentionally empty", moved to per-test fixtures 2026-05-18). No customers, no appointments, no transactional rows. Consequence: after any rebuild, NO tenant is bookable until something installs a business shape — tests use fixtures (`seedDynaTireBusinessConfig`-style); `sim-taskgroup` requires its `SIM_TENANT` to be hand-configured or restored (e.g. business-shape rows copied from prod read-only). Also know your target: `rebuild-db.sh` defaults to the **`postgres`** database (matching the backend's `.env`), while vitest integration tests use **`test_db`**. Every test that needs transactional data **creates it in `beforeAll` and deletes it in `afterAll`** — `feedback_test_isolation.md` is the standing rule. Playwright's `globalSetup` runs `scripts/rebuild-db.sh --yes` once per suite (DROP SCHEMA + apply `supabase/baseline.sql` + seed) so every E2E invocation starts from an identical, validated state. A test failure is provably the code or the test, never "the previous test left a stray row." Origin: 2026-05-18 — seed had accumulated 17 stale appointments + 12 stale customers across `db:seed` re-runs, masking real bugs.
- **Backend code changes require BOTH a rebuild AND a restart to take effect.** Two separate steps, both mandatory: (1) `npm run build` recompiles `src/*.ts` → `dist/*.js`; (2) killing and re-launching `node dist/src/index.js` swaps the running process onto the freshly compiled JS. Skipping either is a silent no-op — restart-only re-runs the stale `dist/`; build-only updates the bytes on disk but the in-memory process keeps the OLD code. Use `npm start` (does both) or surgical `kill $(lsof -ti :4001) && npm run build && node dist/src/index.js &`. Unit tests (`vitest`) DO pick up source changes immediately because they import directly from `src/`, so green unit tests + a stale-binary backend will produce a "false-green source layer / red E2E layer" mismatch that looks like flake but is actually the binary lagging the source. Origin: 2026-05-18 — multi-row INSERT fix landed in `src/`, ran clean through `vitest`, but the E2E suite kept hitting the original deadlock because neither rebuild nor restart had happened in 24 hours. Watch for `ps -eo etime,cmd | grep dist/src/index` to spot a backend that's been running longer than the most recent `src/` mtime suggests.

## Code Conventions

See `docs/workflow/CODING_STANDARDS.md` — canonical source of truth for naming, DB schema, testing (5W/HAPPY/SAD), routes, dashboard patterns, commit messages, review checklist, Prettier/ESLint, function size, and all other standards. CLAUDE.md no longer duplicates them.

## Known Issues

- OpenAI API quota needs monitoring (GPT-4.1-mini for the voice LLM, GPT-4o-mini for summaries/classify, plus embeddings).
- Voice AI filler phrases ("Absolutely!", "Great!") still slip through occasionally despite prompt engineering.

## Project Status

**Phase 13 (Production Readiness) in progress.** Local reruns 2026-09-08, all three suites green: **3,029 passing** (254 files) in the root backend suite (`npm test`), **1,069 passing** (99 files) in dashboard Vitest, and **981 passing** (60 files) in agent Vitest. The 2026-08-18 root-suite failure (`tests/regression/rlsIsolation.test.ts` could not reach `test_db` as `app_user`) is gone — `scripts/start-test-db.sh` + `scripts/setup-test-db.ts` now bootstrap the role, see root `DEVELOPMENT_WORKFLOW.md`. Zero TS errors across backend / agent / dashboard. **32** top-level route modules under `src/routes/` (+ the `agentTools/` module dir). **40 committed Playwright e2e spec files** run on every PR. **196** SQL migrations on disk. Coverage breakdown in `docs/planning/TEST_COVERAGE.md`; security posture in `docs/operations/SECURITY.md`; Railway + observability setup in `docs/operations/DEPLOYMENT.md`. Per-session shipped history lives in `docs/planning/RESOLVED.md`.

**Legal-hold — do NOT merge/enable without owner + legal sign-off:** PR #68 (`POST /customers/:id/purge`, single-customer GDPR/CCPA erasure, kill-switch `ENABLE_CUSTOMER_PURGE`) and PR #69 (automated data-retention worker, `ENABLE_RETENTION_WORKER` + explicit `RETENTION_DAYS`). Both erase PII irreversibly and are inert until enabled. Branches deleted 2026-06-23; restorable from the PR pages. See `docs/planning/TODO.md` → Legal-hold.

**All 3 Railway services (secretary-hq backend, secretary-hq-agent, dashboard) deploy from `main`** (verified 2026-06-12 via Railway GraphQL — each service's latest deployment `meta.branch = "main"`). **Shipping = MERGE to main via PR**, not a branch push. A `git push` to a feature branch deploys NOTHING. Earlier features (greeting #5, call-logging #6) reached prod because their PRs were merged. GitHub branch protection on `main` now gates merges (and thus Railway deploys from `main`) behind green CI (all 4 jobs: Backend, Dashboard, Agent, E2E). The protection rule was applied 2026-06-15 via the updated recommendations in `.github/BRANCH_PROTECTION.md` (require PR + exact 4 status checks + enforce admins + conversation resolution + no direct pushes). Railway's own "Wait for CI" toggle **is enabled on all 3 services** (`checkSuites: true` on every deployment trigger — verified 2026-07-27 via Railway GraphQL; enabled 2026-07-09, see `docs/planning/TODO.md`). **Consequence you must know: a red CI run on `main` makes Railway mark that commit's deployments `SKIPPED`, and SKIPPED is TERMINAL — turning CI green afterwards does NOT make Railway retry, and the skipped deployments report `canRedeploy: false`.** Prod then sits on the previous build while every service reports healthy, which looks exactly like a successful deploy. Not hypothetical: 21ea335 (#298) merged green, the `main` re-run of E2E failed on a Docker Hub outage (`docker pull ankane/pgvector` → `context deadline exceeded`, no test ever ran), and all 3 services skipped. Recovery: re-run the failed job, confirm green, then trigger the deploy explicitly — `serviceInstanceDeployV2(serviceId, environmentId, commitSha)` against `https://backboard.railway.com/graphql/v2` for each of the 3 service ids, or press Deploy in the Railway UI. **After any merge, verify the deploy actually happened** (`/health`'s `started_at` must move; `npm run status -- --env prod`) rather than assuming the merge shipped it. Wait for green CI (use `npm run ci:status`) before merging. Apply prod DB migrations BEFORE the merge.

Remaining blockers: **PSTN inbound reaches the agent** — confirmed 2026-06-30 by a real call to `+1 630-822-9086` (the live number). What is still unverified is the **booking + transfer + preference legs on a live different-carrier call** (`docs/planning/TODO.md` P0 §1.3–1.5). REFER/call-transfer is supported by default on Telnyx FQDN connections (confirmed 2026-07-07 — no toggle needed). `DASHBOARD_URL` on Railway, prod migrations apply (incl. `20260611000000_tenant_forward_phone`). (Paid observability — `SENTRY_DSN` / `BETTER_STACK_TOKEN` — was **decided against** 2026-07-02; the free `/metrics` + Pino stdout + `/ready` stack stands. The code keeps the optional integration if a free path is ever chosen.) See `docs/planning/TODO.md` (P0 Voice) + `docs/operations/RUNBOOK.md` section 7.

## Production

- Backend: `https://secretary-hq-production.up.railway.app/` (`/health` endpoint)
- Public site + dashboard: `https://www.secretaryhq.com` (Railway origin `dashboard-production-cee3.up.railway.app`). Landing page at `/`, "Try live demo" → `/demo` → `POST /demo/start`.
- Phone: **`+1 630-822-9086`** (Telnyx, tenant Thinking Hammer LLC `d5e3c6a1-…`; current). Previous `+1 630-866-1960` (bought + routed 2026-06-02; Telnyx id `2973794140900296302`) dead. Test verification number `+1 630-822-9086`. Old `+1-630-937-9478` dead (order deleted). Provisioning via `POST /provisioning/activate` (search → purchase → assign to SIP Connection `livekit-outbound`).
- Stripe webhook: `https://secretary-hq-production.up.railway.app/billing/webhook` (3 events) — **the route exists and verifies signatures, but NO endpoint is registered at Stripe.** Probed 2026-08-04: `webhook_endpoints` returns zero, and prod's `STRIPE_SECRET_KEY` is an `sk_test` key, so no event has ever been delivered. This line previously read as if billing were wired. Registration steps: `docs/planning/TODO.md` P0 §2 part A.
- Full Railway env-var list, deploy commands, and observability setup in `docs/operations/DEPLOYMENT.md`.
