# TODO — SecretaryHQ (single backlog)

**This is the one and only backlog.** Consolidated 2026-07-05 from the former
`GAPS.md`, `IMPROVEMENT_IDEAS.md`, `IMPROVEMENTS_TODO.md`, and
`AIASSISTANT_GO_LIVE_TODO.md` (all deleted; their done items + analysis archived
verbatim in `docs/planning/RESOLVED.md` under the 2026-07-05 entry).

Items are ordered by what should be done first. Ownership tags:
`(Dale)` = user/ops action, no code · `(code)` = codeable now · `(blocked)` = waiting on an external gate ·
**untagged** = deferred code work (the P3 / UX / doc-hygiene sections — no per-item owner because nothing there is scheduled).

**Not backlogs (left as reusable procedure/reference, do not fold here):**
`docs/workflow/BRANCH_CHECKLIST.md`, `docs/workflow/CODING_STANDARDS.md`, `docs/operations/DEPLOYMENT.md`,
`docs/workflow/DEVELOPMENT_WORKFLOW.md`, `docs/operations/ALERTS.md`. Completed work + history: `docs/planning/RESOLVED.md`.
Voice/Telnyx go-live ops detail + incident recovery: `docs/operations/RUNBOOK.md` §7.

---

## 🔴 HIGH — `/tenants/:id/update-config` call-transfer/disclosure hijack risk (2026-09-16, roady audit)

**Highest-severity finding of the 2026-09-16 audit sweep — called out in its own section, not blended into the list below, because of blast radius.**

- [x] **(code) NEW 2026-09-16 — HIGH: `/tenants/:id/update-config` (and `GET /tenants/:id/config`) have no server-side role check; any authenticated front_desk login can hijack live call transfers and overwrite attested legal disclosure text.** **CLOSED 2026-09-16, PR #522** (along with the staffing-CRUD MEDIUM item below and the full consolidated table further down — one batch). `src/routes/tenants.ts` (~lines 324-376, shifted from ~302-350 by PR #517's unrelated `GET /tenants` column-allowlist fix landing above these routes in the same file — re-verified 2026-09-16, gap itself unchanged): both routes gate only on `requireAuth` + tenant-match-or-super-admin (`req.auth?.tenant_id === id`, else `requireSuperAdmin`-equivalent check) — verified directly against the code, there is no `req.auth.role` check anywhere in either handler. `UpdateConfigSchema` (~lines 50-121) lets **any authenticated tenant user, owner or front_desk, no distinction**, write: `forward_phone` (the live call-transfer destination — every future "talk to a person" call routes wherever this points), `call_disclosure` + `disclosure_attested` (the owner's legally-attested caller-disclosure text, meant to affirm the owner's own state-law compliance — see the schema comment at line ~104), `checklist_preset_id` / `checklist_overrides` (booking_mode, message_mode, disabled conversation blocks — what the AI will and won't do on every call), `tts_voice`/style flags, `owner_phone`, `logo_url`, `business_type`.
  - **Concrete failure:** a front_desk-role account (receptionist login) with a valid JWT can call `POST /tenants/:id/update-config` directly (devtools, a saved request replay, no elevated access needed) and it succeeds today. They could silently redirect `forward_phone` to any number they choose (live call-transfer hijacking — a "talk to a person" caller ends up wherever the front-desk account pointed it, with nothing in the UI to reveal the change), overwrite the legal disclosure text the owner specifically attested to (the attestation stamp `call_disclosure_attested_by`/`_at` records who attested, not who is authorized to change it afterward), or reconfigure what the AI agent does on every single call.
  - The dashboard hides this **client-side only**: `OutlookLayout.tsx`'s `isFrontDeskOnly` (verified — `role === 'front_desk' && !isAdmin`) excludes the `setup`/`ai-insights` tabs from front_desk nav and snaps a stale-URL front-desk session back to `dashboard`. The route itself enforces nothing — the UI restriction is cosmetic, not a security boundary.
  - **Same root-cause pattern as the already-tracked staffing-CRUD gap above** ("MEDIUM: staffing CRUD (employees/shifts/resources) has no server-side owner/role check," from PR #508's audit pass) — both are "dashboard hides the tab, the route doesn't check the role" — but this one is materially higher impact: it reaches live call-routing and legally-attested disclosure text, not just staff/schedule records. **Fix both in the same batch, or at minimum land this one first** given the blast radius (call hijacking + legal-attestation integrity vs. staff-record tampering).
  - **Also found via the same audit, same shape, lower impact — fix together:** `src/routes/services.ts` (create/update/delete — verified: all three gate on `requireTenantId` only, no role check), `src/routes/skills.ts` (create/delete — same), `src/routes/mappings.ts` (all 4 assign/unassign routes — same). These only relink service/staff/skill assignments (same-tenant, reversible, no call-routing or legal-text impact), so they're lower severity than the update-config finding but identical root cause: a route that trusts the dashboard's client-side role hiding instead of checking `req.auth.role` itself.
  - Fix: add `role === 'owner'` gating (or equivalent) to `/tenants/:id/update-config` at minimum — `GET /tenants/:id/config` is lower urgency (a read, not a write) but should get the same review — and to the services/skills/mappings routes listed above, matching the existing pattern already used at `/customers/import` (`src/routes/customers.ts` ~line 243, shifted from ~230 by PR #517's super-admin-scoping fix landing above it in the same file, `role === 'owner'` check with the comment "bulk PII writes are not a front-desk operation").

---

## 🔴 HIGH — `/setup/commit` bypassed all five staffing/catalog owner gates through the wizard's bulk-write path (2026-09-16, roady adversarial re-verify of #522)

- [x] **(code) NEW 2026-09-16 — HIGH: `POST /setup/commit` and `POST /setup/default-service` had zero role check.** **CLOSED 2026-09-16, PR #523.** `src/routes/setup.ts` had no `requireOwnerRole` import at all — every route gated only on `requireTenantId`. `/setup/commit` is the onboarding wizard's bulk write, backed by `insertDraftGraph` (`src/services/setupGraph.ts`) — it inserts/updates services, resources, employees, shifts, and skill mappings from a client-supplied draft graph, and in `mode: 'sync'` it soft-deletes/prunes every service, resource, and employee the draft omits, plus hard-deletes `employee_schedule`/`employee_schedule_pattern`/`service_employee`/`service_resource` rows for anything not in the draft (`setupGraph.ts:314-373`, `420-446`). This is the exact same write surface `services.ts`/`employees.ts`/`resources.ts`/`shifts.ts`/`mappings.ts` were each individually locked to `requireOwnerRole` for in #522 — but this endpoint reached the same tables through a different code path and bypassed all five gates. A front-desk login could POST a crafted draft graph and wipe/rewrite the tenant's entire staff/service structure. Fix: `requireOwnerRole` added to `/setup/commit`, `/setup/default-service`, and `/setup/impact` (the dry-run preview over the same draft — no writes, but exposes the same business-structure detail, gated for consistency with the analogous already-gated `/tenants/:id/config` read). `GET /setup/graph` (the wizard's own precondition read) is unchanged — not part of this finding.
- [x] **(code) NEW 2026-09-16 — MEDIUM: `/square/auth`, `/square/settings/disconnect`, `/square/sync` had zero role check.** **CLOSED 2026-09-16, PR #523.** `src/routes/crmRouteScaffold.ts:75-165` (the shared CRM-OAuth scaffold, registered for Square in `src/routes/square.ts`) gated these three action routes on `requireTenantId` only. Same class of action as `billing.ts`'s checkout/portal and `provisioning.ts`'s activate/deactivate (both correctly owner-gated in #522) — connecting/disconnecting a real external OAuth account is a business-configuration decision, not front-desk. Fix: `requireOwnerRole` added to all three. `GET /square/settings` and `GET /square/sync/status` (reads) left ungated, matching `calendar.ts`'s pattern.
- [x] **(code)** Low-severity cleanup, same PR: `src/routes/customers.ts` (`/customers/import`) and `src/routes/knowledge.ts` (`/knowledge/explain`) each reimplemented `requireOwnerRole`'s exact logic inline instead of calling the shared function — functionally correct but duplicated-logic drift that let the original systemic gap hide for as long as it did. Consolidated both onto the shared `requireOwnerRole`.

---

## Next items to fix (coverage/build/deploy review)

Cold completed items from this section (prod migrations-behind outage, night-shift
morning half, declined-meeting goodbye gate, urgent-message raise-only, wrong-node-id
re-ask, unlinked-service STRICT parity, flaky CI gates) archived in
`docs/planning/RESOLVED.md` (2026-09-14 doc-hygiene entry).

- [x] **TELNYX_PUBLIC_KEY** — **CLOSED 2026-09-14.** Set on Railway `secretary-hq` prod (with API key, SIP connection, phone). Voice path working. Local `.env` may still use a placeholder for dev only.
- **HOLD_LINE filler sound** — not a launch blocker. Watchdog flag can stay on; if filler ever sounds like a stutter on a real call, set `ENABLE_OUTPUT_WATCHDOG=false` or retune. CI cannot grade this.
- [x] **(code) Scheduler List/Day view silently dropped evening appointments in UTC-behind timezones** — **CLOSED 2026-09-16, PR #504** (flagged 2026-09-15 in PR #503). `useSchedulerData.ts` now builds the fetch-range boundary from the actual UTC instant of local midnight instead of appending `Z` to a local date string; regression test forces `process.env.TZ` to a UTC-behind zone so the bug is visible in CI (GH Actions runners default to UTC, where old and new code were byte-identical). Detail: `docs/planning/RESOLVED.md` (2026-09-16 entry).
- [ ] **(Dale) NEW 2026-09-15 — DECISION NEEDED: should admin-provisioned tenants require the same consent attestation self-serve `/register` now has?** Found during roady review of PR #501 (SettingsView UX pass). `POST /tenants/create` (`CreateTenantSchema`, `src/routes/tenants.ts`) creates an immediately-live tenant + owner login (`createTenantWithOwner` / `src/services/tenants/bootstrap.ts` inserts the bcrypt-hashed `users` row in the same transaction as the tenant row) with **no `consent_attested` field at all** — verified directly against the Zod schema. PR #496 (`feat/register-consent-record`, merged 2026-09-15) added and now enforces exactly this attestation on the self-serve `/register` path before creating the equivalent shape of row. This admin/super-admin path produces the same real, immediately-usable credential for someone else's business with zero consent record. Not touched by #501 (a copy/a11y-only PR that happened to edit the same page). This is a real product/compliance call, not a mechanical fix — do not code either option below without Dale picking one first.

  **Option A — require it.** Add the same `consent_attested: z.literal(true)` field to `CreateTenantSchema` that `RegisterSchema` already has, and stamp `legal_consent_attested_at`/`_by`/`_ip`/`_user_agent` the same way `createTenantWithOwner` does today for self-serve signups. Whoever operates `POST /tenants/create` (today: platform super-admin via the SuperAdminDashboard "Create Tenant" form) would have to explicitly attest on the business's behalf before the row is created — same shape, same enforcement point, no new code path.

  **Option B — document the exemption.** State plainly that admin-provisioned tenants are exempt because the platform operator, not the business itself, is creating the row on the business's behalf in a supported-onboarding context (sales-assisted setup, migration, demo-to-real conversion) rather than a self-service signup the attestation was built to gate. If chosen, this needs liability-shift language to exist SOMEWHERE — e.g. in the operator's own onboarding agreement with the business, or a required manual step (signed order form, verbal confirmation logged elsewhere) before an admin runs `POST /tenants/create` — because today nothing fills the gap Option A would otherwise close; the exemption is currently just an absence, not a documented and covered one.

- [x] **(code) NEW 2026-09-16 — `GET /tenants` `SELECT *` now ships PII + secrets to the SuperAdmin tab, verified.** **CLOSED 2026-09-16, PR #517.** Found during roady audit of `SuperAdminDashboard` + the tenant admin surface. `src/routes/tenants.ts:220` — `SELECT * FROM tenants WHERE is_deleted = false ORDER BY sort_order ASC, created_at DESC` — is correctly gated by `requireSuperAdmin` (not a cross-tenant auth hole; not an RLS bypass), but since PR #496 (migration `20260915000000_tenant_registration_consent.sql`) added `tenants.legal_consent_ip` / `legal_consent_user_agent`, this one response now also ships that PII, plus Stripe customer/subscription ids and the full raw `system_prompt`, for EVERY tenant, to the dashboard's SuperAdmin tab — even though no component in that view tree (`TenantCard`/`TenantEditPanel`/`TenantCoreAttributesSection`) renders any of it. Same class of issue this codebase has already fixed elsewhere (`GET /templates/full`'s deliberate column allowlist; the `drop_inert_columns` migration deleting columns a `SELECT *` was shipping with zero readers). Medium severity — over-exposure to an already-authorized super-admin caller, not an auth/RLS bypass. Fix: replace `SELECT *` with an explicit column list matching what the SuperAdmin view tree actually renders.
- [x] **(code) NEW 2026-09-16 — Phone provisioning doesn't check `is_deleted`, so a soft-deleted tenant's "Activate Phone" still works.** **CLOSED 2026-09-16, PR #517.** Found during the same roady audit. `src/services/provisioningService.ts`'s `activatePhone` (`SELECT tenant_id, name, phone_status FROM tenants WHERE tenant_id = $1`, ~line 107) and the `deactivatePhone`/status-read paths (~line 268) query `tenants` directly with no `is_deleted = false` filter — this service isn't routed through `createWithTenantClient`'s soft-delete choke point (the one documented under `20260713000000 tenants_soft_delete` as the enforcement point for exactly this rule). Reachable from `TenantPhoneProvisioning.tsx` inside `TenantEditPanel` on the SuperAdminDashboard. Today, clicking "Activate Phone" against an already soft-deleted tenant purchases a real Telnyx number, assigns it live, and flips `phone_status` — a deleted business's line comes back online for real inbound calls at real cost. `deactivatePhone`/status-read share the same missing filter but are lower risk (releasing/reading a number, not buying one). Medium severity — a missed filter, not an auth hole (only reachable by an already-authorized super-admin). Fix: add `AND is_deleted = false` (or route through the soft-delete choke point) to all three queries.
- [x] **(code) NEW 2026-09-16 — HIGH: `GET /customers` has a code path with zero tenant scoping.** **CLOSED 2026-09-16, PR #517.** Found during a second roady pass over `CRMView`/`CustomerDetailPanel`/`EmployeeManagementView`/`ShiftManagementView`/`ResourceManagerView`. `src/routes/customers.ts` (~lines 149-157): when the effective tenant on a request equals the literal super-admin UUID (`SUPER_ADMIN_TENANT_ID`) — the default state on a fresh super-admin session before a managed tenant is selected, or after "Exit admin mode," or via a bookmarked URL — the handler runs `SELECT * FROM customers WHERE is_deleted = false ORDER BY name LIMIT $1 OFFSET $2` with **no tenant filter and no `requireSuperAdmin` check**, shipping every tenant's customer PII (name/phone/email/address/notes) in one response. Not confined to the Customers tab: `dashboard/lib/hooks.ts`'s `useStaticData()` (used by `EmployeeManagementView`/`ShiftManagementView`/`ResourceManagerView`) unconditionally calls `Api.customers.list(tenantId)` as a side effect even though those screens never render customer data — so simply opening Setup → Staff/Shifts/Resources as a super-admin with no managed tenant selected fires the same cross-tenant request. Not a confirmed active prod leak: `customers`' RLS policy (`20260323000000_force_rls_single_pool.sql`) predates and was never swept into the generic `_admin_bypass` policy, and the raw pool client here has no tenant GUC set — so under `app_user` in prod this likely evaluates to NULL and RLS denies, returning zero rows today. But that safety is incidental to the DB role, not to any check in the handler itself — the same class of gap this codebase's own postmortems (the 2026-05-21 anonymous-`tenant_id` hole, see `/src/middleware/fastify-middleware.ts` entry above) concluded should never be the only defense. Fix: remove the branch, or gate it behind `requireSuperAdmin` + explicit opt-in + an actual tenant label on each row.
- [x] **(code) NEW 2026-09-16 — MEDIUM: staffing CRUD (employees/shifts/resources) has no server-side owner/role check.** **CLOSED 2026-09-16, PR #522.** Same roady pass. `src/routes/employees.ts`, `src/routes/shifts.ts`, `src/routes/resources.ts` gate every route with `requireTenantId` only, no `req.auth.role` check — unlike `customers.ts`'s `/customers/import` route (line ~230), which explicitly requires `role === 'owner'` ("bulk PII writes are not a front-desk operation"). The dashboard UI hides staff/shift/resource management from `front_desk` role users (client-side only), but a front-desk user's own valid JWT can call `POST /employees/create`, `DELETE /employees/:id/delete`, `POST /shifts/blackouts`, etc. directly and it succeeds — same-tenant, so not a cross-tenant break, but an authorization gap inconsistent with the product's own stated model. Same pattern, smaller scope: `DELETE /customers/:id` (line ~547) also has no owner check despite being a destructive, appointment-cancelling action. Not confirmed active exploits, not auth/RLS bypasses in the traditional sense — same-tenant authorization gaps. Fix: add `role === 'owner'` gating (or equivalent) to the mutating routes in these three files, matching `/customers/import`'s existing pattern.
- [x] **(code) NEW 2026-09-16 — HIGH: `GET /appointments` has the identical unguarded super-admin branch as the `GET /customers` finding above.** **CLOSED 2026-09-16, PR #517.** Found during a third roady pass, this time over `AppointmentView`/`VoiceCallsView`. `src/routes/appointments.ts` (~lines 259-313): when `tenantId === SUPER_ADMIN_TENANT_ID`, the query is built with **no `a.tenant_id` filter** (the branch at line ~270 that pushes the tenant condition is skipped) and runs through the raw `withPoolClient` (no tenant context set) rather than `withTenantClient`. The `SELECT` also joins `jsonb_build_object('name', c.name, 'first_name', ..., 'phone', c.phone, 'metadata', c.metadata)` for the appointment's customer, so every matched appointment across every tenant ships its customer's PII in one response. Reachable from `AppointmentView.tsx`'s `fetchAppointments()` (`dashboard/lib/useAppointmentCRUD.ts`), fired on mount — meaning opening the Scheduler/Calendar tab, the most-trafficked screen in the dashboard, as a super-admin with no managed tenant selected fires this. Same incidental-RLS-only protection as `GET /customers`: appointments' RLS policy denies on a NULL tenant GUC under `app_user` in prod, but that is DB-role luck, not an application check, and the same postmortem class this codebase has already flagged (2026-05-21 anonymous-`tenant_id` hole). Fix both `GET /customers` and `GET /appointments` together — identical pattern, two files.
- [x] **(code) NEW 2026-09-16 — MEDIUM: "Send Cancel/Reschedule Links" reports SMS delivery success it cannot verify, because it never checks `ENABLE_SMS`.** **CLOSED 2026-09-16, PR #517.** Same roady pass. `POST /appointments/:id/send-self-service-links` (`src/routes/appointments.ts` ~lines 822-948, triggered by `AppointmentDetailPanel.tsx`'s `handleSendLinks()`) calls `sendSms()` directly with no `ENABLE_SMS` gate — a second, independent SMS-send path never wired to the same false-promise guard the voice agent's tools go through (root `CLAUDE.md` Architecture: SMS is globally OFF pending 10DLC registration; Telnyx accepts the send and reports success anyway, error 40010, and nothing reaches a handset). A staff member clicks the button, `handleSendLinks` shows a green success toast off `res.success` (verified: `AppointmentDetailPanel.tsx` line ~98), and the text never arrives. TCPA consent IS correctly checked before sending (verified: the `consent_records` query at line ~877 gates on `consent_given`/`revoked_at` before any send is attempted) — the gap is purely that a known-impossible-platform-wide delivery is reported as sent. Fix: gate this route on `ENABLE_SMS` the same way the agent tools are gated, or otherwise stop reporting `success: true` for a send that cannot reach a carrier.

### 🔐 Consolidated — no server-side role check is systemic, not isolated (2026-09-16, roady capstone grep sweep) — **CLOSED 2026-09-16, PR #522**

**Root cause, stated once:** across this codebase, dashboard tab visibility (`OutlookLayout.tsx`'s `isFrontDeskOnly` role hiding) has been the _only_ access control on a large class of mutating routes. Every route below assumes the UI hid the button from a `front_desk` login and none of them check `req.auth.role` server-side — so a front-desk JWT (devtools, a saved request replay, no elevated access needed) can call them directly and it succeeds today. This is the **same pattern** already tracked in detail as the staffing-CRUD finding (PR #508, `employees.ts`/`shifts.ts`/`resources.ts`) and the `/tenants/:id/update-config` finding (PR #512, 🔴 HIGH section above) — this entry is the full-surface grep sweep that followed those two, closing out the audit with one list instead of scattering more PRs. All same-tenant authorization gaps (a front-desk user acting past their role on their own employer's tenant), not cross-tenant breaks or RLS bypasses, verified directly against current code:

| Route(s)                                                                                                                                                                                                                                           | File                                                              | Only check present                                                                                                                                                           | What a front-desk JWT can do                                                                                                                                                                                                                                                                                                                                                                                                      |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `POST /provisioning/activate`, `POST /provisioning/deactivate`                                                                                                                                                                                     | `src/routes/provisioning.ts`                                      | none at all — not even `requireTenantId` (tenant-mismatch/anonymous requests are still caught by the global `tenantMiddleware`, but nothing in the route itself checks role) | Purchase a real Telnyx number (real recurring cost, ~$12-13/mo per the SMS section above) or deactivate the tenant's live line, taking inbound calls down                                                                                                                                                                                                                                                                         |
| `POST /billing/checkout`, `POST /billing/portal`                                                                                                                                                                                                   | `src/routes/billing.ts`                                           | `requireTenantId`                                                                                                                                                            | Open a Stripe checkout/portal session for the tenant's billing (`/billing/webhook` is correctly excluded — signature-verified, not user-facing, not part of this finding)                                                                                                                                                                                                                                                         |
| `DELETE /knowledge/:id`, `POST /knowledge/ingest`, `POST /knowledge/add`, `PUT /knowledge/:id`, `PATCH /knowledge/unanswered/:id/resolve`, `POST /knowledge/import-website`, `POST /knowledge/import-document`, `PATCH /knowledge/suggestions/:id` | `src/routes/knowledge.ts`                                         | `requireTenantId` (8 routes — the largest single cluster)                                                                                                                    | Delete or overwrite the policy answers the live voice agent reads from on every call. `POST /knowledge/explain` in the same file is the one route done correctly (`req.auth.role !== 'owner'` check, ~line 553) and is not part of this finding                                                                                                                                                                                   |
| `POST /calendar/settings`, `POST /calendar/settings/disconnect`, `POST /calendar/sync`                                                                                                                                                             | `src/routes/calendar.ts`                                          | `requireTenantId` (`/calendar/sync` doesn't even check that)                                                                                                                 | Rewire or disconnect the tenant's calendar integration                                                                                                                                                                                                                                                                                                                                                                            |
| `POST /records/:table/:recordId/restore-fields`, `.../soft-delete`, `.../restore`, `POST /records/:table/copy-fields`                                                                                                                              | `src/routes/versionHistory.ts`                                    | `requireTenantId`                                                                                                                                                            | Same gap, **different caveat**: verified these routes are not currently wired to any live UI tab — `DeletedRecordsPanel`/`RecordHistoryModal` are referenced only from `dashboard/e2e/version-history-restore.spec.ts`, not from any reachable page (their UX pass is PR #500, still open). This is a live backend contract today with no role gate, not yet UI-triggerable — needs the fix before or alongside #500 wiring it in |
| `POST /tenants/:id/finalize-setup`                                                                                                                                                                                                                 | `src/routes/tenants.ts` (~line 696, shifted from ~675 by PR #517) | `requireAuth` + tenant-match-or-super-admin, no role check                                                                                                                   | Identical gap to the already-tracked `/update-config` finding (🔴 HIGH section above), just not previously itemized on its own                                                                                                                                                                                                                                                                                                    |

Fix for all of the above: add `role === 'owner'` gating (or equivalent), matching the existing pattern at `/customers/import` (`src/routes/customers.ts` ~line 243, shifted from ~230 by PR #517).

**Separate findings from the same pass, `/register` (`src/routes/auth.ts`), not the role-check pattern above:**

- **Medium: `POST /register` has no rate limit.** Verified: `/login` (~line 94), `/forgot-password` (~line 234), and `/reset-password` (~line 295) all carry `{ config: { rateLimit: {...} } }`; `/register` (~line 163) has none. Unauthenticated, does real work per call (2 INSERTs, bcrypt hash, consent UPDATE, template-copy RPC) — floodable, and the existing `409` "account already exists" response (`result.conflictMessage`, ~line 191) becomes an unthrottled email-enumeration oracle.
- **Low: `business_type` is free text with no HIPAA-vertical denylist.** Verified: `RegisterSchema`'s `business_type` (`src/routes/auth.ts` ~line 32) is `z.string().min(1).max(50)` and neither it nor `createTenantWithOwner`/`src/services/tenants/bootstrap.ts` checks the string against `hipaa|dental|veterinary|chiropractic|optometry|medical` or similar (grepped, zero hits). The picker UI normally constrains this via a `<select>` but falls back to free text on a `GET /templates` failure. This product's own Build Principles say HIPAA verticals get "deleted on sight" — this is a real gap in that otherwise-enforced exclusion, reachable via a direct API call bypassing the picker entirely.

**Reviewed, clean:** `FirstRunTour` — no findings. Noted explicitly so the surface is marked reviewed, not skipped.

This closes out the "un-audited surfaces" **security** sweep specifically (see the note on that line under Wizard Phase B follow-ups below) — distinct from jarvis's owner-judgment UX-pass work on the same view names, which remains tracked separately and is not superseded by this entry.

- [x] **(code)** Fix the role-check gap on all routes in the table above (`provisioning.ts`, `billing.ts`, `knowledge.ts` × 8, `calendar.ts` × 3, `versionHistory.ts` × 4 — land alongside or before PR #500 wires the UI to it, `tenants.ts` `finalize-setup`) — **CLOSED 2026-09-16, PR #522.** `requireOwnerRole()` (`src/middleware/fastify-middleware.ts`) added to every route in the table (still un-wired to a live UI tab for `versionHistory.ts`'s 4 routes, per the table's own caveat — the server-side gate is in place regardless of when #500 wires the UI to it).
- [x] **(code)** Add rate limiting to `POST /register` (`src/routes/auth.ts`), matching `/login`/`/forgot-password`/`/reset-password`. **CLOSED 2026-09-16, PR #522.** `{ max: 5, timeWindow: '5 minutes' }`, identical to `/login`.
- [x] **(code)** Add a server-side HIPAA-vertical denylist check on `business_type` in `RegisterSchema` / `createTenantWithOwner`, independent of the dashboard picker. **CLOSED 2026-09-16, PR #522.** New `shared/hipaaVerticalDenylist.ts` (`isHipaaVertical()`), checked in `RegisterSchema`'s `.refine()` (self-serve `/register`) and in `createTenantWithOwner` itself (so `POST /tenants/create`, which has no Zod schema of its own, is covered too).

Detail + full route list: `docs/planning/RESOLVED.md` (2026-09-16 "Systemic no-server-side-role-check audit" entry).

---

## 📞 Live-call follow-ups still open

Shipped series detail: `docs/planning/CALL_FIX_PLAN.md` (2026-07-30 batches) and
`docs/planning/RESOLVED.md` (2026-08-13 job-tree / CALL1+CALL2, 2026-08-15 E2E
observation sweep — all defects closed, sim suites green).

- [x] **(Dale)** Run `scripts/pin-owner-for-hire-preset.sql` against prod after deploy — **DONE 2026-09-14.** Thinking Hammer `checklist_preset_id` → `owner_for_hire_front_desk` (was NULL). `job_inquiries` already had rows; recent calls already selected `job` tree. Re-test call optional belt-and-braces.
- [ ] **(Dale)** Read the first `greeting_spoken` `ms_since_participant` values off
      prod agent logs (event `greeting_spoken` — **not** in Postgres). 2026-09-14 probe: no values in recent Railway agent logs / voice_sessions.metadata. **MEASURE before fixing** on next live call.

---

## 🔴 P0 — Launch blockers (clear before the first paying customer)

Ordered: the product must answer + transfer + book on a real call, then take money,
then be gated/insured. Most of this is your action, not code — the code is shipped.

### 1. Voice path — make a real call work end-to-end

_Post-live voice enhancements (recording disclaimer, etc.) live in **🎙️ Voice — Phase 2** at the bottom of this file._

- [x] **(Dale, second handset)** **Live validation call** — **CLOSED 2026-09-14 (Dale verbal).** Real PSTN calls from a second handset verified answer + dialog on production. Dale also uses live calls as ongoing QA for dialog defects (e.g. double-ask). Checklist below kept as optional regression recipe:
  1. Set the **forward number** on the dashboard AI Persona → "Forward Calls to a Person" (`+1 608 217 5303`) before calling.
  2. Call `+1 630-822-9086` from a second handset (can't call from the forward-to cell and forward to it).
  3. Validate booking: appointment lands in `appointments` for tenant `d5e3c6a1` inside a real shift window.
  4. Validate transfer: say "talk to a person" → your cell rings + Calls tab shows the transcript.
     Code wiring shipped 2026-09-14 (`transfer_call` always-on checklist passthrough when forward number configured).
  5. Validate dialog: agent asks preferred time, widens when none fit, never imposes a slot, recalls preferences across calls.
  6. Validate the booked time is what was said, not an earlier slot (`booked_start` confirmed back, not caller time restated blindly).
     (PSTN inbound itself already confirmed 2026-06-30.)
- [x] **(code)** **Wire `transfer_call` into the question-tree toolset** — 2026-09-14, PR #462. Always-on passthrough via `ALWAYS_ON_PASSTHROUGH_TOOLS` + `offerTransfer` (same forward-number gate as `CLOSER_WITH_TRANSFER`). Not a per-tree action node — every preset gets it when a forward number is configured; omitted entirely when absent.
- [x] **(Dale)** **Delete or merge the duplicate active `Dale DeMott` employee row in prod.** — **CLOSED 2026-09-14.** Prod Thinking Hammer has a single active `Dale DeMott` employee row; no second active/soft-deleted dup found under tenant RLS. App-level 409 guard remains.
- [x] **(code)** **Confirm whether the Aura TTS zero-bytes bug reaches prod.** ~~CLAUDE.md's TTS section documents an unresolved 2026-08-14 finding…~~ **CLOSED 2026-09-14 (PR #467) — dev-host-only artifact, not prod risk.** Ran `cd agent && npm run verify:tts` against the monorepo `.env` Deepgram key after confirming it **fingerprints identical** to Railway `DEEPGRAM_API_KEY` (same sha256_12 / len / prefix+suffix). Result: **10/10 SPEAKS** (6 Aura voices + greeting/hold/recovery/tool-fallback), each path returning ~100k–290k audio bytes over the **WebSocket** speak path. Prod `AURA_TTS_STREAMING` is **UNSET** (streaming default stays on). Keep `greetingPickup.ts` + `AURA_TTS_STREAMING=false` escape hatch + `verify:tts` gate; do not flip the prod default. Evidence: `docs/planning/RESOLVED.md` (2026-09-14 Aura zero-bytes).

### 2. Billing — be able to take money

- [ ] **(Dale)** **Decide final tier pricing** before creating Stripe products — current placeholders ($129/$279) have not been validated. Fold in live cost ledger once a few honest calls exist (CALL2 ~**$0.068**/96s, ~138k input tokens/turn — prior model was ~35× too low). Research findings + cost model (2026-07-07):
  - **Variable cost per call (5-min avg):** Telnyx ~$0.03 + LiveKit ~$0.02–0.05 + Deepgram STT $0.02 + OpenAI LLM ~$0.001 + TTS ~$0.02–0.09 = **~$0.09–0.17/call**
    - ⚠️ **Stale input (flagged 2026-07-28):** the TTS figure is OpenAI's, and TTS moved to **Deepgram Aura** on 2026-07-14. The LLM also moved 4o-mini → **4.1-mini**. Both legs need re-pricing from current provider rates before this model is used to set a price — deliberately NOT guessed here.
  - **Loss point:** an uncapped Solo tier at 1,000 calls costs $90–170 in variable cost alone — near-zero or negative margin at $129/mo
  - **Recommended Solo cap: ~300–400 calls/month** → variable cost ~$27–51, gross margin ~$78–102 on $129/mo
  - **Competitor benchmarks (verified July 2026):** Rosie AI $49/$149/$299 (250/1,000/2,000 min); Goodcall $79/$129/$249/agent (100/250/500 unique customers/mo); Signpost $199/$399/$749 (AI-only → hybrid human+AI)
  - **Key differentiator to keep:** include booking + call transfer at ALL tiers — competitors (Rosie, Goodcall) gate these to mid-tier. Lead with "full receptionist from day one."
  - **Suggested tier shape:** Solo ~$99–129/mo (1 location, ~300 calls/mo cap, full booking+transfer) · Growth ~$199–249/mo (multi-location or higher volume, Square CRM sync, analytics) · Pro ~$349+/mo (unlimited volume, priority support)
  - **Volume metering is built** — answered-call counter + soft-cap enforcement live under `src/services/billingUsage.ts` / voice-session-start; Dale still owns final Stripe price IDs and may retune `PLAN_CAP_*` env after real usage data.
- [ ] **(Dale)** **Stripe setup — part A: test-mode wiring. NO BANK ACCOUNT NEEDED.** A bank account gates **payouts**, not API configuration; every step below works today on the `sk_test` key prod already carries. Test mode has its own separate keys and webhook endpoints, so none of this touches live money. Only the pricing decision above is a real prerequisite (price IDs get baked into env vars).
  1. **Create products + prices** in Stripe **test mode** — Solo, Growth, Pro. Note the 3 price IDs.
  2. **Register the webhook endpoint** in the Stripe dashboard **while it is in TEST MODE** (the toggle top-left — test and live endpoints are separate objects with separate signing secrets): `https://secretary-hq-production.up.railway.app/billing/webhook`, 3 events: `checkout.session.completed`, `invoice.payment_failed`, `customer.subscription.deleted`. Copy the endpoint's signing secret (`whsec_…`) shown after creation.
     - **Status 2026-08-04: NOT registered.** Probed the live account directly — `webhook_endpoints` returns **zero** endpoints, and prod's `STRIPE_SECRET_KEY` is an `sk_test` key. CLAUDE.md's Production section states this URL as if it were wired; it is not. Nothing has ever delivered to it.
  3. **Set 5 env vars on Railway**: `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET` (**the `whsec_` from step 2**), `STRIPE_SOLO_PRICE_ID`, `STRIPE_GROWTH_PRICE_ID`, `STRIPE_PRO_PRICE_ID`.
  4. **Test-mode round-trip** (no real money): trigger a test checkout and verify each event activates/revokes the tenant gate. Events arrive at prod from the endpoint registered in step 2 — nothing else to run. (`./scripts/simulate.sh stripe` path-checks the wiring first.)
     - **`STRIPE_WEBHOOK_SECRET` must match whichever path actually delivers, and there is exactly one signing secret in play at a time.** A registered dashboard endpoint signs with ITS `whsec_`; `stripe listen` signs with a DIFFERENT `whsec_` that the CLI mints per session and prints on startup. Point both at prod with one secret configured and every event down the other path fails signature verification with a 400 — which reads like broken webhook code and is not.
     - **`stripe listen` is an OPTIONAL, LOCAL-ONLY alternative** — use it to exercise the handler against a backend on your own machine, never alongside the registered prod endpoint: `stripe listen --forward-to http://localhost:4001/billing/webhook`, then set `STRIPE_WEBHOOK_SECRET` **locally** to the `whsec_` the CLI prints. Do not forward the CLI at the production URL; prod would receive each event twice, and the copy signed with the CLI's secret would 400.
- [ ] **(Dale)** **Stripe setup — part B: live mode. THIS is what needs the bank account.** Do only after part A's round-trip passes.
  1. **Open an LLC bank account** for Thinking Hammer LLC — required before Stripe can pay out. (Also listed under Legal §5 below.)
  2. **Connect bank account to Stripe** — Stripe dashboard → Settings → Bank accounts & scheduling.
  3. **Re-create products + prices in LIVE mode** — test-mode objects do not carry over. New price IDs.
  4. **Register the webhook again in LIVE mode**, same URL and same 3 events, and copy the new `whsec_`. Live-mode endpoints are separate objects from test-mode ones and sign with their own secret — this must come BEFORE the env swap, because the secret does not exist until the endpoint does.
  5. **Swap the 5 Railway env vars to live values** — live secret key, live price IDs, and the **new** `STRIPE_WEBHOOK_SECRET` from step 4. The moment this lands, the test-mode endpoint's events start failing signature verification; that is expected, and it is why part A must be finished first.
- [ ] **(Dale)** **Stripe Tax** (after round-trip verified): enable Stripe Tax in Stripe dashboard → Tax → Settings; register nexus for IL + customer states; set `STRIPE_AUTO_TAX=true` on Railway. (Code done — `automatic_tax` gated behind the flag.)

### 3. Security housekeeping

- [ ] **(Dale)** **Rotate the Railway team token** created 2026-06-12 — it was pasted into a Claude session. Burn + reissue.
- [ ] **(Dale)** **Rotate the Supabase DB password** — exposed in a session transcript 2026-07-11.

### 4. Legal / business (long lead time — start early)

- [ ] **(Dale)** Open an **LLC bank account** for Thinking Hammer LLC (required before Stripe payouts).
- [ ] **(Dale)** Add **TCPA-compliant SMS opt-in** consent language at booking time — required before any confirmation texts. Copy: `docs/product/TCPA_SMS_CONSENT_COPY.md` (2026-09-16, PR #507) — dashboard checkbox/disclosure + voice-agent spoken-offer script, reusing the existing `consent_records`/`ConsentService` path. **Dale-approved 2026-09-16, pending counsel review**; not yet wired into any UI or prompt.
- [ ] **(Dale)** **E&O insurance** before the first paying customer (~$800–1,200/yr; Next/Hiscox).
- [ ] **(Dale)** **Cyber Liability insurance** before the first paying customer (often bundled with E&O).

---

## 🟠 Legal-hold — built, DO NOT merge/enable without sign-off

Both erase PII irreversibly (kill-switched off / inert until enabled). Branches deleted in the 2026-06-23 cleanup; restorable from the PR pages.

- [ ] **(blocked — legal)** **PR #68** — `POST /customers/:id/purge` owner-gated single-customer GDPR/CCPA erasure (typed phone confirmation, atomic anonymize-in-place + audit_log PII redact, kill-switch `ENABLE_CUSTOMER_PURGE`; 8 tests).
- [ ] **(blocked — legal)** **PR #69** — disabled-by-default automated retention/purge worker (`ENABLE_RETENTION_WORKER` + explicit `RETENTION_DAYS`, no default window, per-tenant-failure-isolated; 9 tests). Broader-PII scope (`voice_sessions`/transcripts/appointment descriptions) is a deliberate follow-up.

---

## 🟡 P1 — Customer success & trust (non-blocking, do after P0)

- [ ] **(Dale)** Verify **reminder delivery stats** in prod. **Unblocked 2026-07-09** — Telnyx creds confirmed, and `TELNYX_PHONE_NUMBER` corrected from the dead `+163****1960` (see P0 §1). Note the stats before that fix were measuring a broken `from` address: fallback-tenant sends were rejected by Telnyx and logged as `status='failed'` in `communications_history`. Expect `sent` now. Check the Failed-only drill-down (`GET /communications/history?status=failed`) and confirm no new failures post-`23:49:38Z`.
- [ ] **(Dale/code)** **Pricing tiers (Pro/Enterprise)** positioning.

### Optional integrations — turn on per business need (code complete, need creds + a live round-trip)

- [ ] **(Dale)** **Google Calendar** — `GOOGLE_CLIENT_ID/SECRET/CALLBACK_URL` + GCP OAuth app; prove a real round-trip via `calendarSync.ts` + `SYNC_TEST_RECORDER`.
- [ ] **(Dale)** **Outlook Calendar** — `OUTLOOK_CLIENT_ID/SECRET/CALLBACK_URL` + Azure app.
- [ ] **(Dale)** **Square CRM** — `SQUARE_CLIENT_ID/SECRET/CALLBACK_URL` + `SQUARE_WEBHOOK_SIGNATURE_KEY` + provider OAuth app (code no-ops safely until set).

---

## 🟢 P2 — Quality, scale & ops visibility

- [x] **(code)** **Volume metering + tier cap enforcement** — monthly answered-call counter from `voice_sessions`; configurable caps via `PLAN_CAP_SOLO` / `PLAN_CAP_GROWTH` / `PLAN_CAP_PROFESSIONAL` / `PLAN_CAP_FREE` (defaults Solo 350, Growth 1000, Pro unlimited, free-tier 50 for null/unknown plan under soft-cap); meter counts soft-deleted billable + active in-flight (no owner-delete wipe, no completed-only TOCTOU); `GET /billing/usage` meter + 80% warn / 100% blocked banners; soft-cap refuse on `/agent-tools/voice-session-start` **and** `/voice/session/start` (`error_code=usage_limit_exceeded`, metric `call_rejected_usage_limit_exceeded`); agent speaks capacity line and ends the call. Toggle off with `USAGE_SOFT_CAP_ENFORCE=false` (pack overage messaging remains). Page `usage_cap_check_failed` / `voice_session_start_failed` rates (fail-open / fail-soft).
- [x] ~~**(Dale/code)** _(Optional)_ Repoint Railway `healthcheckPath` → `/ready`~~ — **DECLINED 2026-09-14 (PR #468).** Keep `railway.json` `healthcheckPath: /health`. `/ready` stays monitoring-only. Rationale + failure modes: `docs/planning/RESOLVED.md` (2026-09-14 Railway healthcheck decision).
- [x] **(code)** **Website-scan re-scan scheduler** — **DONE 2026-09-14 (PR #482).** Migration `20260914000000_tenant_website_scan_tracking` adds `tenants.website_scan_url` + `website_last_scanned_at` + `website_scan_fail_count` + `website_scan_last_attempt_at` (NULL URL = never scanned / opt-out). Shared `importWebsiteKnowledge` supersedes prior open suggestions, stages + stamps in one tenant transaction, resets fail_count on success. Worker `websiteRescanScheduler` (prod ON unless `ENABLE_WEBSITE_RESCAN_SCHEDULER=false`) daily-ticks oldest-stale non-demo tenants, default **30d** stale / **5** per tick / **5** maxFails (env-clamped). Dead-URL exponential backoff + quarantine. Multi-instance advisory lock. Still stages suggestions only — never auto-publishes. No boot stampede. Skip whole tick if no `OPENAI_API_KEY`. Roady HIGH follow-up t_462175ab.

### Structural refactors

- [ ] **(code)** **Migration chain squash** — **203** files in `supabase/migrations/` (2026-09-14). Do when convenient; `baseline.sql` already carries the collapsed schema. **PLAN ONLY 2026-09-14 (PR #459) — unsafe to execute without Dale prod gate** (prod `schema_migrations` head, data-migration/seed parity, role-grant restore, no checked-in chain-delete runbook). Full procedure + entry criteria: [`docs/planning/MIGRATION_CHAIN_SQUASH_PLAN.md`](./MIGRATION_CHAIN_SQUASH_PLAN.md). Do **not** delete migration files until that plan’s §3 criteria are green.

Dashboard component subdirectory migration finished 2026-09-12 (zero loose `.tsx`/`.ts`
at `dashboard/components/`). Backend suite warning cleanup (incl. pg@9 single-client
`Promise.all` break) shipped 2026-09-08. Detail: `docs/planning/RESOLVED.md`.

---

## 🔵 P3 — Moat & expansion (deferred until a customer asks — build principle: no integrations on spec)

- [ ] **Square CRM deeper reads** — pull open jobs into voice context; real external OAuth + Stripe + live CRM round-trips in CI (recorder-only today).
- [ ] **Extended self-service** — public portal/login (manage all appointments); waitlist / callback-queue tool; no-show auto-marking + auto-rebook.
- [ ] **Voice enhancements** — post-call "how did we do?" SMS/NPS link; multi-language; real-time owner listen-in / barge.
- [ ] **Product expansion** — booking widget/embed; granular RBAC beyond owner/front_desk; white-label / reseller theming; public API; PDF + analytics export (CSV export shipped #189); SSO/SAML; international numbers (US-centric today); multi-DID per tenant.
- [ ] **Schedule sub-view consolidation (C1+C2)** — merge the 4 scheduler sub-views (calendar/staff/resources/list) → 2 (calendar Day/Month + Team/Resources) with one unified header. `dashboard/components/SchedulerView.tsx`. (large/UX; from the former IMPROVEMENT_IDEAS.) **Open — needs a UX design pass with Dale before build** (it changes the scheduler layout; brainstorm the target shape first).
- [ ] **Threaded demo mode (E1)** — replace the static `/demo` page with a session flag (`isDemoMode`) injecting read-only sample data into the live dashboard shell (stays in sync with real UI automatically). (large.)
- [ ] **Future CRM/platform candidates** (build-deferred per the `docs/product/STRATEGY.md` vendor heuristic — "how does this vendor make money?") — QuickBooks/Xero, Toast, Apple Calendar (safe infra/transaction partners); Microsoft Teams (notify-only); Vagaro/Mindbody, Acuity/Calendly (competitor-ish → shallow read or import-only).

---

## 🎨 UX backlog (separate workstream — `/ux-expert` audits)

- [x] **(Dale — BLOCKER)** Review live scheduling **coloring/grading** — **ACCEPTED 2026-09-14 (Dale verbal).** Keep current colors/grades. De-grade slices stay reverted; do not strip coloring unprompted. Revisit only if Dale flags a specific surface later.
- [x] **Cluster A — neutral-language / no-grading** — **DEFERRED 2026-09-14 with Dale's color accept.** Same 8 surfaces (`StepReview`, `SkillRelationshipMap`/`SkillMapNode`, `ResourceColumnsView`, `AppointmentListView`, `EmployeeDayFocusPanel`, `AnalyticsView`, `AppointmentDetailPanel`) may still use traffic-light / % / coverage-badge patterns; that is now intentional product choice, not an open de-grade ticket. Re-open only on Dale request.
- [ ] **Wizard Phase B follow-ups, remaining** (explicitly deferred in the design doc, not bugs):
      auto forwarding-verification heuristic (SIP caller-ID match instead of asking the owner) —
      named, not built; real Telnyx porting API integration — deferred until a real port customer
      per YAGNI. (Wizard Phase B + abandoned-test-number reaper shipped — see RESOLVED.md.)
- [ ] **Dense-view decomposition** — remaining over-300 coordination surfaces: `NewSchedulerView` (1582 — do with C1+C2 scheduler consolidation); other over-300 files are unavoidable coordination code (wizard state machines, layout shell, GoLivePanel). Five decomposition slices already shipped 2026-07-05…07 (PRs #201, #211, #212, #217, #218) + coverage batch #219 — detail in RESOLVED.md.
- [ ] Un-audited surfaces — **[REVIEW]** before beta (owner-judgment layout/flow; copy/a11y partials already landed). **The _security_-audit reading of this line is now closed as of 2026-09-16** — the roady capstone grep sweep above (🔐 Consolidated section) plus PR #508 (staffing CRUD), #509 (SECURITY.md customers/staffing), #510 (appointments cross-tenant), and #512 (tenant update-config) collectively covered every named surface's backing routes for server-side auth/role gaps, including `FirstRunTour` (reviewed, clean) and `/register` (two new findings, tracked above). What remains open below is strictly the **owner-judgment UX pass** (layout/flow), tracked separately from and not superseded by the security work — that's jarvis's track, not this one. Most already had a copy/a11y **partial fix** 2026-07-03 plus correctness/a11y defect batch 2026-07-05 (PR #200). `AIConfigView` (PR #492) and `LoginView` + `/forgot-password` + `/reset-password` (PR #499) got their owner-judgment UX pass 2026-09-14/16 — removed from this list. `SettingsView` (PR #501), `ProfileView` (PR #502), `DeletedRecordsPanel` + `RecordHistoryModal` (PR #500), and `AnalyticsView` (PR #511) have since also landed their owner-judgment UX pass, merged to `main` 2026-09-16 — removed from this list. `CRMView` + `CustomerDetailPanel`'s owner-judgment UX pass is in PR #521 (open as of this edit) — removed from the list below once that PR merges:
  - `VoiceCallsView`, `AppointmentView` + `AppointmentDetailPanel` + `AppointmentListSidebar`, `BusinessSettingsView`, `EmployeeManagementView`, `ShiftManagementView`, `ResourceManagerView`, `ServiceAssignmentView`, `SkillMatrixView` + `SkillAssignmentsView` + `SkillRelationshipMap`, `/register`, `SuperAdminDashboard` + tenant cards/forms, `FirstRunTour`.

---

## 🧹 Doc hygiene (mechanical, ongoing — low priority)

- [ ] Continue count-drift passes (route modules / migrations / test numbers) after any new route or migration; keep secondary docs synced. `verify-claude-md.ts` guards Key Directories route/migration counts; the Project Status paragraph is excluded via `stripHistoricalSections()`, so test-count prose there drifts silently unless someone re-runs this pass by hand after a PR adds tests.
      **2026-09-16 pass:** `npm run verify:claude-md` was clean at the start (no drift). Ran against a cascade of ~20 PRs merged since 2026-09-15 (#493 through #517, per `git log --oneline` — mostly docs/hygiene, dashboard UX fixes, a scheduler UTC-boundary bug fix, and — merging mid-pass, after the first two test runs below — PR #517, which fixed 5 of the systemic no-role-check/PII-exposure audit findings this same cascade had surfaced; the rest of that audit's findings, incl. the `/tenants/:id/update-config` 🔴 HIGH section above, are still open). Route modules: **29**, unchanged. Migrations: **204**, unchanged (no migration-worthy schema change landed tonight — every fix, including #517's, was a pure authorization/scoping gate, no schema involved). E2E specs: **40**, unchanged. Live suite totals moved for real this pass: backend **3,205** (268 files, up from 3,194/267), dashboard **1,173** (107 files, up from 1,138/104), agent **1,061** (61 files, unchanged) — the increases land almost entirely on PR #517 (new `tests/routes/customers-superadmin-scope.test.ts` + `tests/routes/tenant-routes.test.ts` + `dashboard/lib/api.superAdminScope.test.ts` + `dashboard/lib/useStaticData.test.tsx`, plus extensions to 3 existing files). Confirmed by running the backend suite THREE times across the pass (two before #517 merged, matching the prior 3,194/267 total; one after, matching the new 3,205/268 total) under heavy concurrent `test_db` contention from other agents' sessions in parallel worktrees (`wt-493`, `wt-jarvis3-secfix`, and the primary checkout all had their own `vitest run` active against the shared `test_db` at points during this pass) — individual runs showed FK-violation failures (`employees_tenant_id_fkey`, `shift_overrides_tenant_id_fkey`, `communications_history_tenant_id_fkey` — a concurrent session's cleanup deleting tenant rows mid-run, not a real regression) and pass-count varied run to run, but **the total test/file count is what these docs track**, and it moved cleanly and only when #517 actually landed, confirming the number is real and not `test_db`-sharing noise (standing rule in `CLAUDE.md`'s Build Principles, "DB starts bare-bones, tests own their data"). Secondary docs synced: CLAUDE.md Project Status, README.md Tests row, `docs/architecture/ARCHITECTURE.md` (Last-verified header + §16.7/18.2/18.3/18.4). Migration count required no changes anywhere (README/ARCHITECTURE/DEPLOYMENT already all read 204). **Line-number citations corrected, not just spot-checked:** PR #517 landed a `GET /tenants` column-allowlist fix and a `GET /customers` scoping fix ABOVE several still-open findings in the same two files (plus `appointments.ts`, `provisioning.ts`), shifting every `~line NNN` citation below the edit point. Re-verified against post-#517 `main` (5e501969) and corrected in both this file and `docs/operations/SECURITY.md`: the 🔴 HIGH `/tenants/:id/update-config` finding (`~lines 302-350` → `~324-376`), `/tenants/:id/finalize-setup` (`~675` → `~696`), every `/customers/import ~line 230` cross-reference (→ `~243`), and SECURITY.md's `DELETE /customers/:id ~line 547` (→ `~567`) — each edit notes the PR that caused the shift so the next pass doesn't have to re-derive it. `knowledge.ts`/`auth.ts` citations were untouched by #517 and re-verified unchanged. Also updated SECURITY.md's `GET /customers` gap entry from open to **FIXED 2026-09-16, PR #517**, since it and the identical `GET /appointments` gap were two of #517's five fixes but SECURITY.md hadn't been touched by that PR's own docs commit (only TODO.md/RESOLVED.md were). `npm run verify:claude-md` clean at the end.
      **2026-09-15 pass:** `npm run verify:claude-md` was already clean at the start of this pass (the Key Directories `supabase/migrations` bullet had been kept current by #496's own migration, landing at **204**) — the drift it caught was zero, but the drift it structurally CANNOT catch (Project Status paragraph, by design excluded from the regex scan) had gone stale exactly as predicted: CLAUDE.md's Project Status prose still said "**203** SQL migrations on disk" — one behind the Key Directories bullet — because the digit sat inside `**203**` markdown-bold markup, and the checker's `(\d+) SQL migrations?` pattern requires the digit immediately adjacent to the words, so bolded numbers silently never match at all (not just historical sections — a second, previously undocumented blind spot in the same checker). Fixed to **204**. Re-ran all three test suites for real current totals (route/migration counts hadn't drifted since #470, but #492/#494/#496/#497 added tests): backend **3,194** (267 files, was 3,147/263), dashboard **1,138** (104 files, was 1,126/104), agent **1,061** (61 files, was 1,048/61). Migration count itself moved **202 → 204** since the last pass (20260914000000 tenant_website_scan_tracking + 20260915000000 tenant_registration_consent). Secondary docs synced to 204 migrations: README.md (Tests-adjacent DB row + repo-tree comment), `docs/architecture/ARCHITECTURE.md` (Last-verified header + DB bullet + repo-tree comment), `docs/operations/DEPLOYMENT.md` (both migration-count mentions in §2). Test-count prose also synced in all three (CLAUDE.md Project Status, README Tests row, ARCHITECTURE §16.7/18.2/18.3/18.4). Deliberately left alone: `docs/planning/LEGAL_HOLD_BACKSTOP_DESIGN.md`'s three "203 migrations" mentions — that doc is a point-in-time design proposal citing the count as of its own drafting, not a live-tracked fact, and rewriting it to 204 would misstate what its own dry-run was actually checked against. `npm run verify:claude-md` clean at the end. Checkbox stays open — pass is ongoing after each route/migration PR.
      **2026-09-14 pass (PR #470):** routes **29** (verify-claude-md definition: top-level `.ts` under `src/routes/` excluding `*Helpers.ts` / `*Scaffold.ts` / tests — prior hygiene notes saying **32** counted helpers/scaffold too); `supabase/migrations/` **202**; e2e specs **40**; loose `dashboard/components/` top-level **0**; tools defined **27** (`agent/src/tools/*.ts`). Live suite totals refreshed: backend **3,147** (263 files), dashboard **1,126** (104 files), agent **1,048** (61 files). Secondary drift fixed: ARCHITECTURE tree still said **92** dashboard Vitest files (→104), ALERTS sizing note still said **~32** route modules (→~29), README Tests row still at 3,144. `npm run verify:claude-md` clean. Checkbox stays open — pass is ongoing after each route/migration PR.
- [x] ~~Trim remaining historical narrative from active docs into `planning/RESOLVED.md` when it goes cold.~~ — **DONE 2026-09-14 (PR #460).** Cold `[x]` postmortems + finished series moved to RESOLVED; open items + unique procedures kept.
